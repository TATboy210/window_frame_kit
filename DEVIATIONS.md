# DEVIATIONS

Ledger of intentional deviations from upstream sources. Every entry
corresponds to a `// FRAME:` or `// DEV:` marker in the code. When merging
upstream (`git merge upstream/main`), check each entry so a deviation is
never silently overwritten by an upstream change.

Upstream anchors refer to the pinned pub-cache versions:
`window_manager-0.5.2` and `bitsdojo_window_windows-0.1.6`.

## Ledger

| # | Upstream anchor (file:line) | This package | Deviation | Rationale | Marker |
|---|-----------------------------|--------------|-----------|-----------|--------|
| 1 | bitsdojo_window_windows-0.1.6/windows/bitsdojo_window.cpp:550-568 | windows/frame_controller.h `FrameAdjustMinMaxInfo` + windows/window_frame_kit_plugin.cpp WM_GETMINMAXINFO graft branch | GETMINMAXINFO unconditional `return 0` → cooperative merge: upstream min/max track sizes still apply (× pixelRatio), maximized additionally clamps ptMaxPosition/ptMaxSize to the monitor work area | bitsdojo bug silently defeats `setMinimumSize` (host-project verified); single package = single min/max owner; maximized borderless content stays inside the work area | `// FRAME:` |
| 2 | window_manager-0.5.2/lib/src/window_manager.dart:9-13,43; lib/src/window_options.dart:3; lib/src/widgets/*.dart imports; test/window_manager_test.dart:3,6; example main/home/integration_test | lib/**, test/**, example/** (all ported Dart files) | Mechanical sed of package-derived strings only: import paths `package:window_manager/`→`package:window_frame_kit/`, barrel file name, channel constant `'window_manager'`→`'window_frame_kit'`, example package/title strings | Package identity carried by package name + channel name (D-01: public API identifiers stay verbatim, zero class renames); machine-proven byte-zero-diff after normalization | n/a (proven by `tool/verbatim_diff.sh`, CI ubuntu leg) |
| 3 | window_manager-0.5.2/windows/window_manager_plugin.cpp:592-597 | windows/window_frame_kit_plugin.cpp tail (lands in 02-02) | `extern "C" WindowManagerPluginRegisterWithRegistrar` export block → `window_frame_kit::WindowFrameKitPlugin::RegisterWithRegistrar` bridge forwarding into the anonymous-namespace implementation | Flutter 3.47 template registers via the CApi shell (registrant four anchors immovable — Pitfall 4); probe build + launch verified | `// DEV:` |
| 4 | window_manager-0.5.2/windows/window_manager.cpp:1 | windows/window_manager.cpp:1 (lands in 02-02) | `#include "window_manager_plugin.h"` → `#include "window_frame_kit_plugin.h"` (residual diff exactly 2 lines) | Skeleton header carries the bridge class declaration; upstream `include/window_manager/window_manager_plugin.h` not ported | none (line count recorded; asserted by verbatim_diff.sh B-section in 02-02) |
| 5 | window_manager-0.5.2/windows/CMakeLists.txt:10-17 + skeleton gtest/test block | windows/CMakeLists.txt (merge, lands in 02-02) | Three-point merge: PLUGIN_SOURCES += `window_manager.cpp`; `_SILENCE_CXX17_CODECVT_HEADER_DEPRECATION_WARNING` on BOTH PLUGIN_NAME and TEST_RUNNER targets; skeleton gtest block + `CMAKE_POLICY_VERSION_MINIMUM 3.5` load-bearing line preserved | Upstream has no native test target — codecvt deprecation vs /W4 /WX needs the define on both targets (Pitfall 5) | n/a (structural merge) |
| 6 | window_manager-0.5.2/example/lib/main.dart:3-5, pages/home.dart:10-11,703,947, integration_test/window_manager_test.dart:6,14,118 | example/lib/**, example/integration_test/** | Package-derived renames (main.dart 3 lines / home.dart 4 lines / integration_test 3 lines) + main.dart 2 new logger-registration lines + new file example/lib/utils/window_event_logger.dart | D-04 real-machine verification carrier: console event log = SC3 evidence source; upstream home.dart:1087 `print` hook preserved as-is | n/a (verbatim_diff.sh main.dart special case filters exactly 2 lines) |

| 7 | window_manager-0.5.2/example/lib/pages/home.dart:9,39,60-100,888-899,1027-1049 | example/lib/pages/home.dart + utils/mouse_passthrough_recovery.dart | 移除 tray listener/menu/init，穿透改为 5 秒限时恢复；平台写入串行化并在销毁时恢复 | 用户 2026-09-05 指令（WINDOWS #7）；不改插件 API/native。恢复测试纳入 CI，移除 tray 依赖及 Linux appindicator；home.dart 归一化摘要由 verbatim_diff.sh 精确固定，其余 16 文件与 C++ 22+2 不变 | `// DEV:`（home.dart import 处） |

| 8 | window_manager-0.5.2/lib/src/window_options.dart:7-31 | lib/src/window_options.dart | `customFrame` optional field appended to `WindowOptions` (constructor + field + doc) | FRAME-01/02/03 entry point: enables frame takeover via `waitUntilReadyToShow` before first show; null default sends no channel call, preserving upstream behavior | `// FRAME:` (constructor line + field doc) |
| 9 | window_manager-0.5.2/lib/src/window_manager.dart:123-163 (waitUntilReadyToShow), tail of class | lib/src/window_manager.dart | (a) `waitUntilReadyToShow` applies `options.customFrame` right after titleBarStyle, before fullscreen/maximized restore; (b) new `setCustomFrame(bool)` method sending `{'isCustomFrame': bool}` | Ordered graft so the native subclass installs before any geometry-affecting state transition; explicit runtime toggle for callers not using WindowOptions | `// FRAME:` (waitUntilReadyToShow block + setCustomFrame method) |
| 10 | window_manager-0.5.2/windows/window_manager_plugin.cpp:135-340 (HandleWindowProc) | windows/window_frame_kit_plugin.cpp HandleWindowProc entry + windows/frame_controller.{h,cpp} | When `customFrame` is enabled the graft branch runs BEFORE the upstream branches and owns WM_NCCALCSIZE (equal-width DPI-scaled NC band on all four sides - real non-client area so edge resize hits are native and the Win11 border/rounded corners survive; maximized clamps to work area; dual-path fullscreen collapses the band), WM_NCHITTEST (defensive fullscreen-only guard, otherwise native), WM_GETMINMAXINFO (cooperative merge, DEVIATIONS #1), WM_SYSCOMMAND/SC_SIZE (fullscreen -> swallowed). Disabled -> every message falls through to upstream verbatim. Includes `windowsx.h` avoidance: window_manager 0.5.2's `IsMaximized()` method collides with windowsx.h function-like macros | FRAME-01/02/03 single-unit graft redesigned 2026-09-05 per user target (self-drawn titlebar PLUS system border + uniform 4-edge resize); zero runner wiring; plugin.cpp residue pinned at 148 by verbatim_diff.sh B-section | `// FRAME:` (graft branch, include, member, destructor) |
| 11 | n/a (new capability, no upstream anchor) | windows/window_frame_kit_plugin.cpp `setCustomFrame` method handler | New MethodChannel branch: locates the FLUTTERVIEW child (FindWindowEx + GW_CHILD fallback), arms FrameController with live state probes (`is_resizable_` / `IsFullScreen()` callbacks), forces SWP_FRAMECHANGED on enable/disable; unknown method -> upstream NotImplemented untouched | Channel contract tested in test/channel_contract_test.dart; enable failure surfaces to Dart as `false` instead of partial state | `// FRAME:` (setCustomFrame handler) |
| 12 | window_manager-0.5.2/example/lib/main.dart:13-20 | example/lib/main.dart WindowOptions | `customFrame: true` added to the demo app's WindowOptions | SC1 cold-start demonstration: window starts borderless on first show, before any interaction; pinned by verbatim_expected/example_main.diff | `// FRAME:` (customFrame line) |
| 13 | window_manager-0.5.2/linux/window_manager_plugin.cc:44-50 + macos .../WindowManager.swift:81-95 | linux/window_frame_kit_plugin.cc + macos/.../WindowManager.swift + WindowFrameKitPlugin.swift | `setCustomFrame` handlers on both ports: Linux snapshots `gtk_window_get_decorated` and restores it on disable (plus the `_is_frameless` dead-field fix, `// DEV:`); macOS snapshots styleMask/titleVisibility/titlebarAppearsTransparent and restores all three, keeping the `.titled` bit so the window stays key-capable (XPLAT-04) | Reversible decoration toggle per 04-01-PLAN step 3; no silent-true for unsupported methods — both ports register the same channel contract | `// FRAME:` (both ports) + `// DEV:` (Linux dead-field fix) |

*(Row above is a placeholder to be replaced with the real entry when the
Phase 3 graft lands. New rows append below — one row per functional block,
never per line.)*

## Recording rules

1. Every `// FRAME:` (grafted code) or `// DEV:` (deviation from ported
   upstream code) marker in the source MUST have exactly one ledger row.
2. Upstream anchors use the pinned pub-cache version's `file:line`.
3. Entries are recorded per functional block, not per line.
