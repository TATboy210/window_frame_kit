name: window_frame_kit
# D-03 + WINDOWS #5：description 精简至建议长度内（原措辞 188 字符扣 pana
# 0/10），保留 frameless / window management / desktop 关键词。
description: >-
  Frame takeover and full window management for Flutter desktop (Windows,
  Linux, macOS): frameless windows, native 4-edge resizing, and
  window-manager parity APIs.
version: 0.2.0
# D-01：三字段同指 github.com/TATboy210/window_frame_kit（one-way 决策，落盘即锁）
homepage: https://github.com/TATboy210/window_frame_kit
repository: https://github.com/TATboy210/window_frame_kit
issue_tracker: https://github.com/TATboy210/window_frame_kit/issues

# D-04：topics 全量 5 个（pub.dev 上限 5 个）
topics:
  - desktop
  - window
  - window-manager
  - frameless
  - custom-titlebar

environment:
  # Phase 4 按依赖链复核放宽（骨架期保守值与验证环境一致、零虚报——研究 Pitfall 8）
  sdk: ^3.13.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  # 依赖白名单（XPLAT-02 骨架期生效条款）：直接依赖仅以下两项，任何 win32 出现即违规
  path: ^1.9.1
  screen_retriever: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      linux:
        pluginClass: WindowFrameKitPlugin
      macos:
        pluginClass: WindowFrameKitPlugin
      windows:
        pluginClass: WindowFrameKitPluginCApi

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
