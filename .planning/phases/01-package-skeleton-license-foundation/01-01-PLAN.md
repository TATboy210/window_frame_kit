---
phase: 01-package-skeleton-license-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - macos/window_frame_kit.podspec
  - lib/window_frame_kit.dart
  - lib/window_frame_kit_platform_interface.dart
  - lib/window_frame_kit_method_channel.dart
  - test/window_frame_kit_test.dart
  - test/window_frame_kit_method_channel_test.dart
autonomous: true
requirements:
  - PUB-01
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "三平台插件骨架完整落盘：lib/windows/linux/macos/example/test 目录齐全，pub get / analyze / format / test 四门全绿（SC1 的可自动化半边）"
    - "MethodChannel 名在 Dart/Windows/Linux/macOS 四处一致为 window_frame_kit（SC4，channel 层由包名自动派生，研究 Pattern 1）"
    - "dart pub publish --dry-run 输出 Package has 0 warnings.（SC5 的本机可跑半边）"
    - "依赖白名单成立：直接依赖仅 path、screen_retriever、plugin_platform_interface，pubspec.lock 中零 win32 包条目（SC4 / XPLAT-02 骨架期生效条款）"
  artifacts:
    - path: "pubspec.yaml"
      provides: "包身份（name/description/version/homepage/repository/issue_tracker/topics/sdk）+ 依赖白名单"
      contains: "window_frame_kit"
    - path: "lib/window_frame_kit_method_channel.dart"
      provides: "MethodChannel('window_frame_kit') 常量（channel 红线的 Dart 侧锚点）"
      contains: "MethodChannel('window_frame_kit')"
    - path: "macos/window_frame_kit.podspec"
      provides: "真实 summary/homepage/author 的 macOS podspec（占位符清零）"
    - path: "example/"
      provides: "三平台可运行宿主（Plan 03 实机启动验证与 CI native build 的载体）"
  key_links:
    - from: "pubspec.yaml"
      to: "https://github.com/TATboy210/window_frame_kit"
      via: "homepage/repository/issue_tracker 三字段同指 D-01 仓库"
      pattern: "github\\.com/TATboy210/window_frame_kit"
    - from: "lib/window_frame_kit_method_channel.dart"
      to: "windows/window_frame_kit_plugin.cpp"
      via: "channel 名由包名自动派生，四处一致（研究 Pattern 1）"
      pattern: "window_frame_kit"
---

<objective>
三平台 Flutter 插件骨架落地，并让骨架期质量门与发布门一次全绿：`flutter create` 模板全量生成、format 漂移消除、pubspec 身份四字段 + topics（D-01/D-03/D-04）、依赖白名单生效（XPLAT-02 骨架期生效条款）、`dart pub publish --dry-run` 0 warnings。

Purpose: 这是本 phase 的 tracer 纵切——从空仓库到"可发布检查全绿"的最短完整通路。ROADMAP Phase 1 的 SC1（骨架）与 SC4/SC5 的本机半边在此交付；后续计划（合规基座、构建门、CI）都在这条已验证的通路上叠加。
Output: 完整插件骨架（git 已跟踪）+ 合规 pubspec + 0-warnings 发布门（本机半边）。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package-skeleton-license-foundation/01-CONTEXT.md
@.planning/phases/01-package-skeleton-license-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 三平台插件骨架落地 + 骨架期质量门全绿</name>
  <precondition>D:/flutter/bin/flutter 存在且 `D:/flutter/bin/flutter --version` 报 3.47.x stable；D:/window_frame_kit 是 git 仓库（master 分支）且除 .planning/、.claude/ 外无源码文件</precondition>
  <files>scaffold 全树（lib/window_frame_kit.dart、lib/window_frame_kit_platform_interface.dart、lib/window_frame_kit_method_channel.dart、test/window_frame_kit_test.dart、test/window_frame_kit_method_channel_test.dart、windows/、linux/、macos/、example/、analysis_options.yaml、.gitignore、README.md、CHANGELOG.md、LICENSE 模板版）</files>
  <read_first>
    - .planning/phases/01-package-skeleton-license-foundation/01-RESEARCH.md（§Architecture Patterns 的模板 91 文件清单、§Common Pitfalls 5、§Standard Stack Installation 命令形态）
    - .claude/CLAUDE.md（项目编码规范——骨架代码须遵守）
    - scaffold 产物 pubspec.yaml（结构与 format 漂移核对对象）
  </read_first>
  <action>
    在仓库根 D:/window_frame_kit 执行 scaffold：`D:/flutter/bin/flutter create --template=plugin --platforms=windows,linux,macos --org com.example .`（研究已在真实环境验证该命令形态；包名从目录名 window_frame_kit 自动派生）。随后立即执行 `D:/flutter/bin/dart format .` 一次——研究 Pitfall 5 实测模板有 3 个 Dart 文件与当前 formatter 漂移，不做这步则后续 format 门第一天就红。然后 `D:/flutter/bin/flutter pub get` 生成 pubspec.lock。

    逐项核对模板结构（对照研究 §Architecture Patterns 清单）：lib 三件套（facade/platform_interface/method_channel）、windows/linux/macos 三平台原生壳、example 宿主（publish_to 'none' + path 依赖 ../）、test/ 两个 Dart 测试、analysis_options.yaml（include flutter_lints + analyzer exclude 三平台目录）、.gitignore（含 /pubspec.lock、.dart_tool/）。

    channel 名核对四处（研究 Pattern 1：包名自动派生，本任务只验证不手改）：lib/window_frame_kit_method_channel.dart 的 `MethodChannel('window_frame_kit')`、windows/window_frame_kit_plugin.cpp（约 :23）、linux/window_frame_kit_plugin.cc（约 :69）、macos/window_frame_kit/Sources/window_frame_kit/WindowFrameKitPlugin.swift（约 :6）。

    跑骨架期质量门三连：`D:/flutter/bin/flutter analyze`（期望 No issues found!）、`D:/flutter/bin/dart format --set-exit-if-changed --output=none .`（期望 exit 0）、`D:/flutter/bin/flutter test`（期望全部通过）。

    边界：本任务不改 pubspec 身份字段与依赖清单（Task 2 负责）、不碰 README/LICENSE 内容（Plan 02 负责）、不加 CMake 补丁（Plan 03 负责）、不升级 gtest URL（研究 Pitfall 1 推荐一行方案，属 Plan 03）；不引入白名单外任何依赖。
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && D:/flutter/bin/flutter analyze && D:/flutter/bin/dart format --set-exit-if-changed --output=none . && D:/flutter/bin/flutter test</automated>
    <fails_when>任一命令非零退出；analyze 输出含 "issues found"；format 报告有文件将被重排（exit 1）；test 汇总行是 "Some tests failed."</fails_when>
    <automated>cd /d/window_frame_kit && grep -c "MethodChannel('window_frame_kit')" lib/window_frame_kit_method_channel.dart && grep -c "window_frame_kit" windows/window_frame_kit_plugin.cpp linux/window_frame_kit_plugin.cc macos/window_frame_kit/Sources/window_frame_kit/WindowFrameKitPlugin.swift</automated>
    <fails_when>任一文件计 0（grep 非零退出）——channel 名未在四处一致派生</fails_when>
  </verify>
  <done>三平台骨架完整落盘且 git 已跟踪；四 channel 位一致为 window_frame_kit；analyze/format/test 三门全绿（SC1 可自动化半边 + SC4 channel 名交付）</done>
  <acceptance_criteria>
    - lib/、windows/、linux/、macos/、example/、test/ 目录存在且 `git ls-files` 可见（已提交跟踪）
    - MethodChannel 四处锚点各有命中：lib/window_frame_kit_method_channel.dart、windows/window_frame_kit_plugin.cpp、linux/window_frame_kit_plugin.cc、macos/window_frame_kit/Sources/window_frame_kit/WindowFrameKitPlugin.swift
    - `D:/flutter/bin/flutter analyze` 输出 "No issues found!"
    - `D:/flutter/bin/flutter test` 输出 "All tests passed!"
    - `D:/flutter/bin/dart format --set-exit-if-changed --output=none .` exit 0
    - pubspec.lock 已生成且 gitignored（.gitignore 含 /pubspec.lock）
  </acceptance_criteria>
</task>

<!-- planner-discipline-allow: win32 -->
<task type="auto">
  <name>Task 2: pubspec 身份字段（D-01/D-03/D-04）+ 依赖白名单（XPLAT-02 骨架期生效）+ dry-run 0 warnings</name>
  <files>pubspec.yaml, macos/window_frame_kit.podspec</files>
  <read_first>
    - pubspec.yaml（Task 1 之后的模板态——在现状上编辑，不整文件重写）
    - macos/window_frame_kit.podspec（占位字段现状）
    - .planning/phases/01-package-skeleton-license-foundation/01-RESEARCH.md（§Code Examples Example 2 已验证的 0-warnings pubspec 形态；§Common Pitfalls 7/8；§Open Questions 1/2）
    - .planning/phases/01-package-skeleton-license-foundation/01-CONTEXT.md（D-01/D-03/D-04）
  </read_first>
  <reversibility rating="one-way">homepage/repository/issue_tracker 指向 github.com/TATboy210/window_frame_kit（D-01）：pub.dev 包页与已发布版本永久链接首次上传的 repository URL，改仓库名即断链历史。决策已由用户在讨论期锁定，本任务仅落盘执行，不重设检查点。</reversibility>
  <action>
    pubspec.yaml 一次补齐以下字段（研究 Example 2 是已验证的 0-warnings 最小完备形态）：
    - description（D-03：英文、覆盖 frameless / frame takeover / window management / events / Windows / Linux / macOS 关键词，采用研究验证过的措辞）："Frame takeover and complete window management events for Flutter desktop apps (Windows, Linux, macOS): frameless windows, four-edge native resizing, and full window-manager parity APIs."
    - homepage: https://github.com/TATboy210/window_frame_kit
    - repository: https://github.com/TATboy210/window_frame_kit
    - issue_tracker: https://github.com/TATboy210/window_frame_kit/issues
    - topics（D-04 全量 5 个，顺序照 D-04 原文）：desktop、window、window-manager、frameless、custom-titlebar
    - environment.sdk 保留模板值 ^3.13.2 并在其上加一行 YAML 注释"Phase 4 按依赖链复核放宽"（研究 Pitfall 8 建议：骨架期保守值与验证环境一致、零虚报，最终 floor 锁定属 Phase 4/XPLAT-01）
    - dependencies 白名单三项：path ^1.9.1、screen_retriever ^0.2.2、plugin_platform_interface ^2.0.2；在 plugin_platform_interface 行加 YAML 注释说明其是模板平台接口层、非 win32、Phase 2 移植后复核删除（研究 Open Question 2 的裁量执行——白名单意图是零 win32，模板件不算违规）；不得声明任何其他依赖
    - version 保留模板 0.0.1；name 保留 window_frame_kit

    macos/window_frame_kit.podspec 占位清理（研究 Pitfall 7）：s.summary 改为与 pubspec description 同义的英文一句话；s.homepage 改为 https://github.com/TATboy210/window_frame_kit；s.author 改为真实作者（用 `git config user.name` 与 `git config user.email` 的实值构造）；清除其余 'Your Company'、example.com 类占位字样；不动 s.source 与构建配置字段。

    改完执行 `D:/flutter/bin/flutter pub get` 刷新 pubspec.lock，然后跑发布门本机半边 `D:/flutter/bin/dart pub publish --dry-run`（研究已验证补齐四字段后输出 "Package has 0 warnings."），并留白名单证据：`! grep -Eq 'win32' pubspec.lock` 与 `! grep -Eq '^\s+win32' pubspec.yaml`（后者锚定依赖键行，pubspec 注释行不误伤）。
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && D:/flutter/bin/dart pub publish --dry-run && ! grep -Eq 'win32' pubspec.lock && ! grep -Eq '^\s+win32' pubspec.yaml</automated>
    <fails_when>dry-run 非零退出或汇总行不是 "Package has 0 warnings."；或两个否定 grep 任一命中（锁文件/pubspec 依赖键出现白名单外依赖）</fails_when>
    <automated>cd /d/window_frame_kit && grep -c "TATboy210/window_frame_kit" pubspec.yaml && grep -c "screen_retriever" pubspec.yaml && grep -c "plugin_platform_interface" pubspec.yaml && grep -c "topics:" pubspec.yaml</automated>
    <fails_when>首个计数小于 3（homepage/repository/issue_tracker 未全部指向 D-01 仓库），或任一后续计数为 0</fails_when>
  </verify>
  <done>pubspec 身份五组字段就位且 dry-run 0 warnings；依赖白名单成立且零 win32 证据在案；podspec 无占位符（SC4 + SC5 本机半边 + XPLAT-02 骨架期生效条款交付）</done>
  <acceptance_criteria>
    - pubspec.yaml 同时含 description/homepage/repository/issue_tracker/topics 五组字段；topics 恰为 D-04 的 5 个值
    - 直接依赖恰为 path、screen_retriever、plugin_platform_interface 三个；pubspec.lock 中无 win32 包条目
    - `D:/flutter/bin/dart pub publish --dry-run` 以 "Package has 0 warnings." 收尾
    - `grep -c "TATboy210/window_frame_kit" pubspec.yaml` ≥ 3
    - macos/window_frame_kit.podspec 的 summary/homepage/author 均为真实值（无 'Your Company'、example.com 残留）
    - environment.sdk 保留 ^3.13.2 且带 Phase 4 复核注释
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub 工具链 → 本仓库 | scaffold 与 pub get 引入的模板代码、依赖树进入仓库（供应链入口） |
| pubspec 字段 → pub.dev 包页 | 身份字段决定包页呈现的真实性与可溯源性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | pubspec.yaml 依赖声明 | high | mitigate | 依赖白名单硬门：直接依赖仅 path/screen_retriever/plugin_platform_interface；`! grep -Eq 'win32' pubspec.lock` 留证据（XPLAT-02 骨架期生效条款）。Package Legitimacy Audit 四包全部 Approved，无 [ASSUMED]/[SUS]，故本阶段无需合法性 checkpoint（T-01-SC 不适用） |
| T-01-02 | Spoofing/Repudiation | pubspec 身份字段 | medium | mitigate | homepage/repository/issue_tracker 三字段指向 D-01 真实仓库；`dart pub publish --dry-run` 0 warnings 作为机器门禁（研究 Security Domain：包页误导 pattern 的标准缓解） |
| T-01-03 | Information Disclosure | 发布凭据 | low | accept | 本阶段无 publish 步骤、CI 不保存发布凭据；publisher token 仅存在于发布者本机 keyring（研究 Security Domain V14 既定边界） |
| T-01-04 | Tampering | flutter create 模板代码 | low | accept | 官方 SDK 工具链生成、研究已逐文件检视（91 文件清单）；CI 从源码编译验证（Plan 04），无第三方 codegen 参与 |

注：本阶段无运行时攻击面（无用户输入、无网络、无存储）；主风险为供应链相邻项（依赖白名单完整性），如上如实裁剪。
</threat_model>

<verification>
- `D:/flutter/bin/flutter analyze` 0 issue；`D:/flutter/bin/dart format --set-exit-if-changed --output=none .` exit 0；`D:/flutter/bin/flutter test` 全过
- `D:/flutter/bin/dart pub publish --dry-run` 输出 "Package has 0 warnings."
- channel 名四处 grep 全命中 window_frame_kit
- `! grep -Eq 'win32' pubspec.lock` 成立（白名单证据）
- `git ls-files` 可见 lib/windows/linux/macos/example/test 骨架
</verification>

<success_criteria>
- SC1（骨架落盘）的可自动化半边交付：三平台目录齐备、四门全绿（实机启动半边由 Plan 03 交付）
- SC4 全量交付：channel 名 window_frame_kit 四处一致 + 依赖白名单（仅 path/screen_retriever/plugin_platform_interface，零 win32）
- SC5 本机半边交付：dry-run 0 warnings（CI/pana 半边由 Plan 04 交付）
- D-01/D-03/D-04 三个锁定决策全部落盘
</success_criteria>

## Artifacts this phase produces（本计划范围）

- 新文件：`flutter create` 生成的完整插件树（约 91 文件）——lib/ 三件套、windows/、linux/、macos/ 平台壳、example/ 宿主、test/ 两个 Dart 测试、analysis_options.yaml、.gitignore、CHANGELOG.md、README.md 与 LICENSE 的模板版本
- pubspec.yaml 字段：name=window_frame_kit、description（D-03 措辞）、version=0.0.1、homepage/repository/issue_tracker（D-01 URL）、topics×5（D-04）、environment.sdk=^3.13.2、dependencies={path ^1.9.1, screen_retriever ^0.2.2, plugin_platform_interface ^2.0.2}、dev_dependencies 模板值
- macos/window_frame_kit.podspec：真实 summary/homepage/author
- MethodChannel 常量名 `window_frame_kit`（四处，包名派生）
- 本计划生成但不编辑内容的文件：README.md、LICENSE（模板版，由 Plan 02 整体替换）、windows/CMakeLists.txt（由 Plan 03 打补丁）

<output>
Create `.planning/phases/01-package-skeleton-license-foundation/01-01-SUMMARY.md` when done
</output>
