---
phase: 01-package-skeleton-license-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PUB-01
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "push 到 master 与所有 PR 触发三平台并行 CI：每平台 analyze+format / flutter test / native build，pana 仅 ubuntu 且带通过线阈值（D-05/D-06/D-07/D-08）"
    - "github.com/TATboy210/window_frame_kit 存在且 master 已推送，origin remote 指向它（D-01）"
    - "CI 首跑三 leg + pana 全绿；pana 基线分数已记录且 threshold 已按基线钉死（D-08；研究 Open Question 4 的 runner 假设 A2/A3 核对完成）"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "三平台 matrix checks job + pana ubuntu job（D-05~D-08 载体）"
      contains: "windows-latest"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "github.com/TATboy210/window_frame_kit"
      via: "push(master) + pull_request 触发（D-06）"
      pattern: "pull_request"
    - from: "pubspec.yaml"
      to: ".github/workflows/ci.yml pana job"
      via: "pana 对身份字段评分——Plan 01 的 dry-run 0 warnings 在 CI 侧的持续守护"
      pattern: "exit-code-threshold"
---

<objective>
发布管线接入 CI 并把仓库上线：三平台全量矩阵 workflow（D-05~D-08）、github.com/TATboy210/window_frame_kit 建仓推送（D-01）、CI 首跑全绿并把 pana threshold 按真实基线钉死。

Purpose: SC5 的 CI 半边——pana 与发布检查从骨架期开始被持续守护（Pitfall 10 预防动作）；D-01 建仓是 one-way 决策的落盘执行（pubspec 三字段已指向该 URL）。研究 Open Question 4 的 runner 假设（A2/A3）在此首跑核对。
Output: .github/workflows/ci.yml + 在线仓库（origin 已推）+ 基线钉死的 pana 门。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-package-skeleton-license-foundation/01-CONTEXT.md
@.planning/phases/01-package-skeleton-license-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI workflow 落盘（D-05/D-06/D-07/D-08）</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-package-skeleton-license-foundation/01-RESEARCH.md（§Code Examples Example 3 workflow 骨架、§Common Pitfalls 4、§State of the Art——action 版本选择、§Assumptions A2/A3）
    - .planning/phases/01-package-skeleton-license-foundation/01-CONTEXT.md（D-05/D-06/D-07/D-08）
  </read_first>
  <action>
    新建 .github/workflows/ci.yml（单文件三平台 matrix——CI 文件拆分属 Claude's Discretion，单文件最简）。以研究 Example 3 为骨架，落成具体值：
    - name: ci；触发（D-06）：on push branches [master] + on pull_request（不限分支）
    - job `checks`（D-05 三平台并行）：strategy.fail-fast: false；matrix os: [windows-latest, ubuntu-latest, macos-latest]；runs-on 取 matrix.os
    - 公共步骤（D-07 检查①②）：actions/checkout@v5（研究 SotA：勿抄上游的 v2/v3）；subosito/flutter-action@v2，with channel: stable、flutter-version: "3.47.2"、cache: true（钉版本保证可复现）；`flutter pub get`；`flutter analyze`；`dart format --set-exit-if-changed --output=none .`；`flutter test`
    - native build 步骤（D-07 检查④，按 matrix.os 条件分派）：windows-latest → `flutter build windows --release`；ubuntu-latest → 先 `sudo apt-get update && sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev`（对齐上游 WM 的依赖清单，本包骨架不需要其额外的 liblzma/libayatana 项）再 `flutter build linux --release`；macos-latest → `flutter build macos --release`
    - job `pana`（Pitfall 4：pana 0.23.19 在 Windows 本机必崩，只进 ubuntu）：runs-on ubuntu-latest；checkout + flutter-action 同上；`dart pub global activate pana`；运行 `dart pub global run pana:pana --exit-code-threshold 60 .`，并在该步骤上方写 YAML 注释：60 为骨架期临时通过线（即分数下限 100/160），首跑记录真实基线后按"threshold = 满分 − 基线"钉死（D-08 通过线而非满分级；若 pana 汇总报告的满分非 160，以报告值为准）
    - 明确不加入：publish 步骤、任何 secrets 引用、melos/第三方 pana 包装 action（研究 SotA 弃用清单）

    边界：本任务不触发 push（建仓推送在 Task 2）；workflow 语法正确性由 Task 2 的首跑验证。
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && grep -Eq 'windows-latest' .github/workflows/ci.yml && grep -Eq 'ubuntu-latest' .github/workflows/ci.yml && grep -Eq 'macos-latest' .github/workflows/ci.yml && grep -Eq 'exit-code-threshold' .github/workflows/ci.yml && grep -Eq 'pull_request' .github/workflows/ci.yml && grep -Eq 'flutter-version: "3.47.2"' .github/workflows/ci.yml && echo WORKFLOW_OK</automated>
    <fails_when>任一模式未命中或未输出 WORKFLOW_OK——三平台 matrix、pana 阈值、触发条件、版本钉死任一缺失</fails_when>
  </verify>
  <done>ci.yml 落盘：三平台 matrix + 每平台四项检查 + pana 仅 ubuntu 带临时通过线（D-05~D-08 结构交付）</done>
  <acceptance_criteria>
    - jobs.checks 为三 OS matrix 且 fail-fast: false
    - 每平台步骤含 analyze、format --set-exit-if-changed、flutter test、对应平台 native build
    - pana job 仅 runs-on ubuntu-latest，运行命令含 --exit-code-threshold
    - 触发含 push branches [master] 与 pull_request
    - action 版本为 actions/checkout@v5 与 subosito/flutter-action@v2，flutter-version 钉 "3.47.2"
    - 全文件无 secrets、无 publish 步骤
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 建仓推送 + CI 首跑全绿 + pana 基线钉死（D-01 + D-08 闭环）</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-package-skeleton-license-foundation/01-RESEARCH.md（§Assumptions A1/A2/A3/A5、§Open Questions 4、§Standard Stack 的 gh CLI 行、§Don't Hand-Roll 的 pana threshold 语义）
    - .planning/phases/01-package-skeleton-license-foundation/01-CONTEXT.md（D-01 one-way 警示、D-06 触发时机、D-08 通过线语义）
    - .github/workflows/ci.yml（Task 1 产物——threshold 回填对象）
  </read_first>
  <precondition>gh CLI 已认证：`gh auth status` 显示 TATboy210 登录且 token 含 repo、workflow scope（规划时点已验证在位，keyring 凭据）——D-01 建仓推送是执行器唯一无法在无凭据时代办的环节，未认证时停下建 human checkpoint，不自行伪造。</precondition>
  <reversibility rating="one-way">仓库 URL 是 pub.dev 包页与已发布版本永久链接的目标（D-01 原始评级）；决策已由用户锁定，本元素仅留评级记录，不重设检查点。</reversibility>
  <action>
    第一步（认证前置门）：重跑 `gh auth status` 核对执行时点仍已登录 TATboy210 且 token 含 repo+workflow scope。已认证 → 全自动继续；未认证或 token 过期 → 停下建 human checkpoint 请用户 `gh auth login`（或设置 GH_TOKEN 环境变量），确认后重试本步。执行器不持有、不落地任何凭据明文。

    第二步（建仓 + 推送，D-01 one-way 落盘）：执行 `gh repo create TATboy210/window_frame_kit --public --source=. --remote=origin --push`——一条命令完成 建空仓 + 配 origin + 推 master（gh 认证协议为 https，remote 会落成 https URL）。若命令失败：回退路径 = 请用户在 https://github.com/new 网页建空仓（public，不勾选 README/LICENSE/.gitignore 任何初始化项——避免远端多出与本地历史冲突的首个 commit），执行器随后 `git remote add origin https://github.com/TATboy210/window_frame_kit.git` + `git push -u origin master`。推送前逐字核对仓库名与 URL 同 pubspec 的 repository/homepage/issue_tracker 三字段值一致（Plan 01 已按 D-01 落盘）——one-way 决策，pub.dev 首次上传会永久链接该 URL，拼写错误 = 断链历史。

    第三步（CI 首跑观察，D-06 + 研究 A2/A3 核对）：`gh run list --workflow ci --limit 3` 确认 push 触发了 ci workflow（D-06 落地证据）；取 run id 后 `gh run watch <run-id> --exit-status` 等待三 leg + pana 全部结束。同步核对研究假设：A2（windows-latest 的 runner 侧 CMake 版本——gtest 坑是否复现；Pitfall 1 补丁 Plan 03 已入库应自动生效）、A3（linux/macos 模板自足一次通过；ubuntu 的 apt 依赖清单是否齐）。任何红 leg：读该 leg 日志按输出修复——windows 命中 native_assets INSTALL 类错误则在 workflow 的 windows 分支构建步前加 `mkdir -p example/build/native_assets/windows`（研究 Pitfall 3 的 CI 变体）；命中其他首跑问题按日志修复。每次修复 commit + push 重跑，直至三 leg + pana 四项全绿。

    第四步（pana 基线钉死，D-08 闭环）：pana 语义（研究 §Don't Hand-Roll，pana --help 原文核实）：`(max - granted) > threshold` 时非零退出。从首跑 pana 日志读出真实 granted 分数与满分（若满分非 160，以报告值为准），按 D-08 公式计算 threshold = 满分 − 基线，编辑 .github/workflows/ci.yml 把 Task 1 的临时值 60 替换为该计算值，并同步更新旁边 YAML 注释：记录基线分数、满分、钉死日期。commit + push 让 pana 重跑至绿。若首跑 pana 即绿（临时 60 恰好通过）也必须做本步——把 threshold 从临时值钉到真实基线值，防止后续基线漂移时 CI 静默放行更低分；Task 1 的临时 60 无论如何不得原样存活到本任务完成。

    边界：不配置任何 secrets、不建 publish job、不建 master 以外分支、不碰 upstream remote（D-09 属 Plan 02 范围）。
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && git remote get-url origin | grep -q "github.com[:/]TATboy210/window_frame_kit" && test "$(git branch --show-current)" = "master" && gh run list --workflow ci --limit 1 --json conclusion --jq '.[0].conclusion' | grep -qx success && grep -Eq 'exit-code-threshold [0-9]+' .github/workflows/ci.yml && echo CI_GREEN</automated>
    <fails_when>origin 未指向 D-01 仓库、master 未推送、最近一次 ci run 非 success（含 in-progress 结论为空）、或 ci.yml 中 threshold 无数值——任一命中即未闭环</fails_when>
  </verify>
  <done>origin 指向 D-01 仓库且 master 已推送；CI 首跑三 leg + pana 全绿（研究 A2/A3 核对完成）；pana threshold 已按"满分 − 基线"钉死且基线分数已记录（D-08 闭环）</done>
  <acceptance_criteria>
    - git remote get-url origin 指向 github.com/TATboy210/window_frame_kit（D-01）
    - master 已推送至 origin；gh run list 最近一次 ci workflow run 的 conclusion = success（三 leg + pana 全绿）
    - ci.yml 的 --exit-code-threshold 值来自真实首跑基线（满分 − 基线，D-08），不再是 Task 1 的临时 60；YAML 注释记录基线分数与钉死日期
    - 研究假设 A2/A3 的核对结论已留痕（红项若命中已修复并记录处置）
    - workflow 全文件无 secrets、无 publish 步骤（与 Task 1 边界一致）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub runner → 仓库内容 | runner checkout 仓库自身内容构建；workflow 零 secrets 引用，runner 不持有任何凭据 |
| pub 生态 → runner 构建环境 | runner 侧 `flutter pub get` 拉取依赖树（供应链相邻面，非新增面） |
| GitHub 仓库身份 → pub.dev 包页 | D-01 repository URL 在首次发布后永久链接（one-way） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Information Disclosure | workflow secrets / publish 凭据 | low | mitigate | 结构性排除：Task 1/2 边界明令不加入任何 secrets 引用与 publish 步骤；analyze/test/build/pana 全部无需凭据；publish token 永不进 CI（研究 Security Domain V14） |
| T-01-12 | Tampering | 依赖树（runner 侧 pub get） | low | mitigate | 依赖白名单完整性由 Plan 01 落盘的 dry-run 0 warnings + win32 零出现复核把关；CI 只是持续复跑同一棵已审计的白名单树，无新增依赖面 |
| T-01-13 | Tampering | CI action 供应链（actions/checkout、subosito/flutter-action） | low | accept | 两 action 均官方/生态标准且经研究 SotA 核实（@v5 / @v2，大版本钉死）；未采用第三方 pana 包装 action（研究弃用清单）；提交 hash 级钉死留 Phase 5 发布管线规划时按需评估 |
| T-01-14 | Spoofing | 仓库身份（D-01 URL 落盘错误） | medium | mitigate | Task 2 第二步推送前逐字核对仓库名/URL 与 pubspec 三字段一致；one-way 决策（pub.dev 首传永久链接 repository URL），错误 URL = 断链历史，故设前置核对门而非事后修 |

注：CI 无 secrets、无 publish 凭据、无运行时攻击面（无用户输入/网络服务/存储）。以上为发布管线供应链相邻项的如实裁剪。ASVS L1：medium → mitigate（推送前核对门）；low → mitigate（结构性排除已有）/ accept 并记录理由。
</threat_model>

<verification>
- Task 1 grep 门全过：三 OS 标识、exit-code-threshold、pull_request、flutter-version "3.47.2" 钉死均在 .github/workflows/ci.yml 中命中
- `git remote get-url origin` 指向 github.com/TATboy210/window_frame_kit 且 master 已推送（D-01）
- `gh run list --workflow ci --limit 1 --json conclusion` 最近一次 run = success（三 leg + pana 全绿；D-06 触发即由此验证）
- ci.yml 的 threshold 值 = 满分 − 首跑基线（D-08），YAML 注释记录基线分数与日期，临时 60 已被替换
</verification>

<success_criteria>
- SC5 的 CI 半边交付：三平台全量矩阵 workflow 从骨架期持续守护 analyze/format/test/native build/pana（D-05/D-06/D-07）
- D-01 落盘：github.com/TATboy210/window_frame_kit 存在、master 已推、origin 指向它——one-way 决策执行完毕且 URL 与 pubspec 三字段逐字一致
- D-08 闭环：CI 首跑全绿，pana 基线分数已记录，threshold 钉死为"满分 − 基线"（通过线而非满分级）
- 研究假设 A2/A3 首跑核对完成，Open Question 4 关闭（runner 环境事实落定并回写 SUMMARY）
</success_criteria>

## Artifacts this phase produces（本计划范围）

- .github/workflows/ci.yml：三平台 matrix checks job + pana ubuntu job（Task 1）；threshold 值钉死为真实基线并记录基线分数（Task 2）
- origin remote → https://github.com/TATboy210/window_frame_kit + master 推送（D-01 载体；远端仓库本体为外部产物，不落本地文件）
- pana 基线记录：基线分数 / 满分 / 钉死日期（以 ci.yml YAML 注释元数据形式留痕，随 workflow 入库）

<output>
Create `.planning/phases/01-package-skeleton-license-foundation/01-04-SUMMARY.md` when done（须记录：pana 首跑基线分数与钉死后的 threshold 值、A2/A3 核对结论及任何红项处置、D-01 建仓推送的最终 URL）
</output>
