---
phase: 01-package-skeleton-license-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - windows/CMakeLists.txt
autonomous: true
requirements:
  - PUB-01
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "example 在 Windows 本机以 release 配置编译产出 window_frame_kit_example.exe（SC1 的可自动化半边；D-07 检查④的本机执行）"
    - "example 实机启动并显示窗口（SC1 的人工半边——宿主 UAT 证据标准适用：控制台无崩溃不等于 UI 正确呈现）"
    - "VS2026 捆绑 CMake 4.x 环境下 example 可正常 configure（研究 Pitfall 1 的一行补丁生效）"
  artifacts:
    - path: "windows/CMakeLists.txt"
      provides: "CMAKE_POLICY_VERSION_MINIMUM 3.5 补丁（研究 Pitfall 1 已验证解法，置于 googletest FetchContent 之前）"
      contains: "CMAKE_POLICY_VERSION_MINIMUM"
  key_links:
    - from: "windows/CMakeLists.txt"
      to: "example 的 native 构建"
      via: "example 构建时引用插件 CMake，补丁使 gtest 1.11 块在 CMake 4.x 下可 configure（研究 Pattern 4）"
      pattern: "CMAKE_POLICY_VERSION_MINIMUM 3\\.5"
    - from: "example/build/.../window_frame_kit_example.exe"
      to: "实机窗口"
      via: "exe 启动 → 用户视觉确认（人工半边）"
      pattern: "window_frame_kit_example"
---

<objective>
打通 SC1 的构建半边与人工半边：给模板 Windows CMake 打 Pitfall 1 一行补丁，example 以 release 配置在本机编译出 exe，并启动供用户实机确认窗口显示。

Purpose: 研究 Pitfall 1/2/3 是执行者裸跑模板必然撞上的三个构建阻断（本机 VS2026 + CMake 4.3.1 已实测复现），全部解法已验证；SC1 的"example 在 Windows 实机启动显示窗口"是无头 CI 无法判定的人工项（宿主 UAT 证据标准），由本计划交付。
Output: 打补丁的 windows/CMakeLists.txt + example 的 Windows release 构建产物 + 实机启动确认（人工半边走 UAT 收集）。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-package-skeleton-license-foundation/01-CONTEXT.md
@.planning/phases/01-package-skeleton-license-foundation/01-RESEARCH.md
@.planning/phases/01-package-skeleton-license-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CMake Pitfall-1 补丁 + example Windows release 构建</name>
  <files>windows/CMakeLists.txt</files>
  <read_first>
    - windows/CMakeLists.txt（模板态——定位 FetchContent_MakeAvailable(googletest) 调用位置与 PLUGIN_SOURCES 清单）
    - example/windows/CMakeLists.txt（确认 set(include_window_frame_kit_tests TRUE) 门控在位——研究 Pattern 4）
    - .planning/phases/01-package-skeleton-license-foundation/01-RESEARCH.md（§Common Pitfalls 1/2/3、§Architecture Patterns Pattern 3——PLUGIN_NAME/目标名不可改的 tooling 约束）
  </read_first>
  <action>
    第一步（补丁）：在 windows/CMakeLists.txt 的 `FetchContent_MakeAvailable(googletest)` 调用之前插入一行 `set(CMAKE_POLICY_VERSION_MINIMUM 3.5)`，并附研究 Pitfall 1 给出的英文注释（说明 googletest 1.11.0 声明的 cmake_minimum_required 低于 3.5、被 VS2026 捆绑的 CMake 4.x 直接拒绝，此行恢复兼容）。研究 Pitfall 1 已实测该行使 configure 通过。不改 PLUGIN_NAME、不改目标名、不动 PLUGIN_SOURCES 清单（模板注释明言 tooling 依赖这些名字）。

    第二步（构建前置）：`mkdir -p example/build/native_assets/windows`——研究 Pitfall 3 实测全新插件 example 缺该目录会 INSTALL 失败（native assets 问题族）。

    第三步（构建）：`cd example && D:/flutter/bin/flutter build windows --release`。本机 VS2026（generator "Visual Studio 18 2026"）+ CMake 4.3.1-msvc1 环境下应产出 exe。

    失败处置纪律（研究 Pitfall 2，必须照做）：若构建在 INSTALL 步骤报 "file cannot create directory: C:/Program Files/window_frame_kit_example" 或 cmake_install/MSB3073 字样——这是首次 configure 失败毒化缓存的信号，执行 `rm -rf example/build` 全清，重做第二步 mkdir，再执行第三步构建。不要在毒化缓存上反复重试。若命中其他首跑错误，按输出修复后同样全清 example/build 再编。

    注意 /W4 /WX：模板给所有目标开了警告即错误——本任务只加一行 CMake 变量、不引入任何 C++ 代码，不应产生新告警。
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && grep -c "CMAKE_POLICY_VERSION_MINIMUM 3.5" windows/CMakeLists.txt && test -f example/build/windows/x64/runner/Release/window_frame_kit_example.exe && echo BUILD_OK</automated>
    <fails_when>补丁行计数为 0，或 exe 路径不存在（构建未成功产出），或未输出 BUILD_OK</fails_when>
  </verify>
  <done>CMake 补丁在位且 example 的 Windows release 构建产出 window_frame_kit_example.exe（SC1 构建半边 + D-07 检查④本机执行）</done>
  <acceptance_criteria>
    - windows/CMakeLists.txt 在 FetchContent_MakeAvailable(googletest) 之前含 set(CMAKE_POLICY_VERSION_MINIMUM 3.5)（恰一次）
    - PLUGIN_NAME 与插件目标名保持模板原值
    - example/build/windows/x64/runner/Release/window_frame_kit_example.exe 存在
    - 构建过程无 MSB3073 / "Program Files" INSTALL 报错残留（命中过则已按 Pitfall 2 处置后重建成功）
    - 若 CI 首跑（Plan 04）在 GitHub runner 上不复现 gtest 坑，补丁保留——研究 Pitfall 1 明言该行对本机 VS2026/CMake4 环境必需
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 启动 example 供实机确认（SC1 人工半边，UAT 收集项）</name>
  <files>（无文件修改——启动构建产物并记录人工确认）</files>
  <read_first>
    - example/build/windows/x64/runner/Release/window_frame_kit_example.exe（Task 1 产物——启动对象）
    - .planning/phases/01-package-skeleton-license-foundation/01-VALIDATION.md（Manual-Only Verifications 表——实机启动的判定标准）
  </read_first>
  <action>
    以后台方式启动 Task 1 产出的 exe（不 flutter run、不挂调试器——直接启动 release 产物，最接近用户真实场景）：在 Bash 中后台执行 example/build/windows/x64/runner/Release/window_frame_kit_example.exe，等待约 5 秒后用 PowerShell 验证进程存活：`powershell -NoProfile -Command "if (Get-Process window_frame_kit_example -ErrorAction SilentlyContinue) { exit 0 } else { exit 1 }"`。

    进程存活后保持窗口打开，等待用户实机确认（human-check，verify-phase 会收集为本 phase UAT 项）。确认完毕后结束进程（`powershell -NoProfile -Command "Stop-Process -Name window_frame_kit_example -ErrorAction SilentlyContinue"`）。

    判定标准（VALIDATION.md Manual-Only 表）：窗口出现且可见、默认插件示例 UI 可见可交互、关闭窗口正常退出。宿主 UAT 证据标准适用——控制台无崩溃不等于 UI 正确呈现，必须以用户实机目视为准。
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Get-Process window_frame_kit_example -ErrorAction SilentlyContinue) { exit 0 } else { exit 1 }"</automated>
    <fails_when>exit 1——启动 5 秒后进程不存在（启动即崩或秒退）</fails_when>
    <human-check>
      1. 观察屏幕：window_frame_kit_example 窗口是否出现且完整可见
      2. 与窗口交互：默认插件示例 UI 是否可见、可点击响应
      3. 关闭窗口：点关闭按钮后窗口是否正常退出（无卡死、无崩溃弹窗）
      回复 "approved" 表示三项全过；否则描述看到的问题。
    </human-check>
  </verify>
  <done>exe 进程实机存活，用户目视确认窗口显示与交互正常（SC1 全量交付，人工半边进入 UAT 收集）</done>
  <acceptance_criteria>
    - 启动后 5 秒进程存活（Get-Process 非空）
    - 用户确认：窗口出现且可见、示例 UI 可交互、关闭正常退出（human-check 收集）
    - 确认后进程被清理，不残留后台实例
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 构建工具链 → 构建产物 | CMake/VS 工具链状态决定产物可信度（本地开发面，非运行时面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | CMake 构建缓存 | low | mitigate | 研究 Pitfall 2 处置纪律写入任务 action：INSTALL 类异常必先 `rm -rf example/build` 全清再重建，禁止在毒化缓存上重试——防止"看似构建成功、实为脏缓存产物" |
| T-01-10 | DoS | 构建脚本副作用 | low | accept | 本任务仅新增一行 CMake 变量、不引入代码与网络访问；构建产物在 gitignore 的 build/ 内，不进版本库；无供应链新面 |

注：本阶段无运行时攻击面（无用户输入、无网络、无存储）；构建门的风险全部是工具链可复现性问题（研究 Pitfall 1/2/3），如上如实裁剪。ASVS L1：low → mitigate（已有已验证处置流程）/ accept 并记录理由。
</threat_model>

<verification>
- `grep -c "CMAKE_POLICY_VERSION_MINIMUM 3.5" windows/CMakeLists.txt` = 1
- `test -f example/build/windows/x64/runner/Release/window_frame_kit_example.exe`
- 启动后进程存活（PowerShell Get-Process）
- 用户目视确认窗口显示/交互/关闭三项（human-check → UAT 收集）
</verification>

<success_criteria>
- SC1 全量交付：骨架完整落盘（Plan 01）+ Windows 实机启动显示窗口（本计划人工确认）
- D-07 检查④的本机半边（flutter build windows --release）通过；三平台 native build 的 CI 半边由 Plan 04 交付
- 研究 Pitfall 1/2/3 的已验证解法全部在本机走通并留痕（补丁行 + 处置纪律）
</success_criteria>

## Artifacts this phase produces（本计划范围）

- windows/CMakeLists.txt：`set(CMAKE_POLICY_VERSION_MINIMUM 3.5)` 一行补丁（含说明注释，位于 googletest FetchContent 之前）
- example 的 Windows release 构建产物 window_frame_kit_example.exe（gitignored，不入库）
- SC1 人工半边的实机确认记录（进入本 phase UAT）

<output>
Create `.planning/phases/01-package-skeleton-license-foundation/01-03-SUMMARY.md` when done
</output>
