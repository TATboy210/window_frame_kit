---
phase: 02-windows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # --- lib/ 逐字区（替换模板三件套）---
  - lib/window_frame_kit.dart
  - lib/window_frame_kit_platform_interface.dart   # DELETE
  - lib/window_frame_kit_method_channel.dart       # DELETE
  - lib/src/window_manager.dart
  - lib/src/window_listener.dart
  - lib/src/window_options.dart
  - lib/src/resize_edge.dart
  - lib/src/title_bar_style.dart
  - lib/src/utils/calc_window_position.dart
  - lib/src/widgets/drag_to_move_area.dart
  - lib/src/widgets/drag_to_resize_area.dart
  - lib/src/widgets/virtual_window_frame.dart
  - lib/src/widgets/window_caption.dart
  - lib/src/widgets/window_caption_button.dart
  # --- 根 pubspec（F-3 白名单收敛）---
  - pubspec.yaml
  # --- test/（D-02：上游壳 + 契约测试；模板双测删除同 commit）---
  - test/window_frame_kit_test.dart                # DELETE
  - test/window_frame_kit_method_channel_test.dart # DELETE
  - test/window_manager_test.dart
  - test/channel_contract_test.dart
  # --- example/ Dart 面（D-04 载体）---
  - example/lib/main.dart
  - example/lib/pages/home.dart
  - example/lib/utils/config.dart
  - example/lib/utils/window_event_logger.dart
  - example/integration_test/plugin_integration_test.dart # DELETE
  - example/integration_test/window_manager_test.dart
  - example/images/tray_icon.ico
  - example/images/tray_icon.png
  - example/images/tray_icon_original.ico
  - example/images/tray_icon_original.png
  - example/pubspec.yaml
  - example/pubspec.lock
  - example/windows/flutter/generated_plugin_registrant.cc
  - example/windows/flutter/generated_plugins.cmake
  - example/linux/flutter/generated_plugin_registrant.cc
  - example/linux/flutter/generated_plugins.cmake
  - example/macos/Flutter/GeneratedPluginRegistrant.swift
  # --- 证明体系 + CI + 账本 ---
  - tool/verbatim_diff.sh
  - .github/workflows/ci.yml
  - DEVIATIONS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [CAPB-01, CAPB-02, CAPB-03, CAPB-04, CAPB-05, CAPB-06, CAPB-07, CAPB-08, CAPB-09, CAPB-10, CAPB-11, CAPB-12]
estimate:
  tokens: 65000
  raw_tokens: 45000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "公开 API 标识符与上游逐字相同（windowManager 单例 / WindowManager / WindowListener / WindowOptions / ResizeEdge / TitleBarStyle）——D-01 costly 决策的执行形态，落盘即锁"
    - "flutter analyze 全仓 0 issue（root analyze 覆盖 example）；flutter test ≥4 用例全绿（契约测试与模板测试删除同 commit，Pitfall 2）"
    - "tool/verbatim_diff.sh 对 pub cache window_manager-0.5.2 跑通：17 个 Dart 逐字区文件 sed 归一化后零差（main.dart 先滤掉恰 2 行 logger 注册）"
    - "channel 名 window_frame_kit 四锚一致（Dart lib/src/window_manager.dart:43 + windows/linux/macos native 锚点不动）；双 lockfile 零 win32；pubspec 白名单收敛为 path + screen_retriever（F-3）"
    - "CI 三 leg 绿：format 门双轨化（逐字区豁免+新码区正名单）、ubuntu apt 增 libayatana-appindicator3-dev、ubuntu leg 增 verbatim-diff 步"
  artifacts:
    - lib/src/**（12 文件逐字区，D-01 全保真 API 面）
    - test/window_manager_test.dart（上游壳，逐字区）+ test/channel_contract_test.dart（新码区，D-02 叠加层，~12-15 用例）
    - example 三件 + integration_test + 4 张 tray 图标 + window_event_logger.dart（D-04 载体，新码区）
    - tool/verbatim_diff.sh（SC5 机器门，本计划落 A 段 Dart；B 段 C++ 由 02-02 扩展）
    - DEVIATIONS.md 5 行新条目 + .planning/WINDOWS.md known-stub 行（example/test/widget_test.dart，Open Q4）
    - ci.yml 三处改动（本 phase 唯一 CI delta）
  key_links:
    - "example/lib/main.dart → windowManager.ensureInitialized() → waitUntilReadyToShow(WindowOptions) → WindowEventLogger 注册（+2 行）"
    - "lib/src/window_manager.dart:43 MethodChannel('window_frame_kit') ↔ windows/linux/macos native 同名锚点（windows 本计划后仍为骨架，02-02 接通实现）"
    - "契约测试双向机制：setMockMethodCallHandler（Dart→native 捕获）+ handlePlatformMessage（native→Dart 注入驱动 15 hook）"
    - "ci.yml verbatim-diff 步 → curl pub.dev 0.5.2 archive → bash tool/verbatim_diff.sh <解包目录>"
    - "example/pubspec.yaml 四依赖 ↔ 三平台 registrant 再生成件（+TrayManagerPlugin）必须同 commit（Pitfall 13）"
  prohibitions:
    - statement: "零嫁接码：不得出现任何 NCCALCSIZE/WM_NCHITTEST/GETMINMAXINFO 修改或 `// FRAME:` 标记（phase 退出门条款）"
      status: resolved
      verification: "! grep -rn 'FRAME:' lib windows example test tool"
    - statement: "channel 名红线：不得复用 'window_manager' 作为 channel 字符串（Dart 常量 + native 三锚点保持 window_frame_kit）"
      status: resolved
      verification: "grep -c \"MethodChannel('window_frame_kit')\" lib/src/window_manager.dart = 1 且 grep -rn \"MethodChannel('window_manager')\" lib test = 空"
    - statement: "零 win32 Dart 依赖（XPLAT-02 骨架期生效条款，example 扩依赖后仍成立）"
      status: resolved
      verification: "grep -c win32 pubspec.lock example/pubspec.lock = 0/0"
    - statement: "逐字纪律：移植面文件不得 opportunistic 重构/重排/reformat/修 quirk（含 IsWindows11OrGreater 恒真、getTitleBarHeight 物理 px——全部 verbatim+记账）；改动只走 sed 脚本"
      status: resolved
      verification: "bash tool/verbatim_diff.sh <pub-cache> 退出 0"
    - statement: "lib/ 保持 verbatim：事件日志等验证辅助只落 example 侧（D-04 零新增 UI，屏上面板已否决）"
      status: resolved
      verification: "window_event_logger.dart 位于 example/lib/utils/；lib/ 下无 logger/debugPrint 新增"
    - statement: "不动区：linux/ macos/ native 模板壳（F-4，channel 锚 :69/:6 保持）、windows/ C++ 面（02-02 处理）、plugin_c_api.cpp / include c_api.h / pubspec pluginClass（registrant 四锚）、pana 门 threshold 10"
      status: resolved
      verification: "git diff --stat 本 commit 不含 linux/window_frame_kit_plugin.cc、macos/**、windows/**、pubspec plugin 段"
---

<objective>
把 window_manager 0.5.2 的 Dart 全量面（lib 12 文件 1926 行 + test 壳 + example 三件 + integration_test + 4 图标）以机械 cp+sed 流水线逐字移植进本包，同 commit 落定证明体系（契约测试 + verbatim_diff.sh + CI 双轨 format 门）——原子单元，不可分 commit（模板 example 引用旧 facade，lib 换血后分 commit 必 CI 红，RESEARCH Pattern 5 硬约束）。

**用户故事（planner 按 ROADMAP Goal 框定）：** 作为 Flutter 桌面开发者，我要 window_manager 0.5.2 的完整 API 面在 window_frame_kit 包名下逐字可用且机器可证一致，以便 Phase 3 嫁接落在已证对等的地基上、宿主迁移≈只换 import 行。

**Purpose:** SC5 的 Dart 半边 + CAPB-01~12 的 facade/契约层全部落地；本 phase 的 tracer（实机窗口出现）在 02-02 由 C++ 面接通。
**Output:** 单一原子 commit「feat(02-01): port window_manager 0.5.2 Dart surface verbatim + proof harness」，CI 三 leg 绿。
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-windows/02-CONTEXT.md
@.planning/phases/02-windows/02-RESEARCH.md
移植源（逐字对照基准，只读）：`C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2/`
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Dart 全量面机械移植流水线（lib + pubspec + test 换血 + example Dart 面 + registrant 再生成）</name>
  <files>files_modified 中 lib/**、pubspec.yaml、test/**（除 channel_contract_test.dart）、example/**（除 window_event_logger.dart 与 tool/CI/账本）</files>
  <read_first>
    - 02-RESEARCH.md §Code Examples Example 1（移植脚本全文，probe 实证）+ §File-by-File Port Map A/C/D（逐文件改动行数 = 验收数值）+ Pitfall 3（barrel import 双重替换，sed 顺序先长串后短串）+ Pitfall 13（生成件同 commit）
    - 移植源 pub cache 目录（上列 canonical 路径）：lib/window_manager.dart、lib/src/**、test/window_manager_test.dart、example/lib/{main.dart,pages/home.dart,utils/config.dart}、example/integration_test/window_manager_test.dart、example/images/*
    - 现骨架：lib/window_frame_kit_platform_interface.dart、lib/window_frame_kit_method_channel.dart、test/ 模板双测、pubspec.yaml:30-34（白名单注释 + plugin_platform_interface）、example/pubspec.yaml
  </read_first>
  <action>
    按 RESEARCH Example 1 脚本执行（可内联跑，不入库；入库的是 Task 2 的 verbatim_diff.sh）：
    1. DELETE 模板五件：lib/window_frame_kit_platform_interface.dart、lib/window_frame_kit_method_channel.dart、test/window_frame_kit_test.dart、test/window_frame_kit_method_channel_test.dart、example/integration_test/plugin_integration_test.dart
    2. lib 面：cp 上游 barrel → lib/window_frame_kit.dart（字节一致）；cp lib/src 全部 11 文件（含 utils/ widgets/ 子目录）；sed 顺序 = 先 `s|package:window_manager/window_manager.dart|package:window_frame_kit/window_frame_kit.dart|g` 再 `s|package:window_manager/|package:window_frame_kit/|g`；channel 串 `s|MethodChannel('window_manager')|MethodChannel('window_frame_kit')|`（仅 lib/src/window_manager.dart:43）。预期改动行数：src/window_manager.dart=6、window_options=1、drag_to_move_area=1、drag_to_resize_area=2、virtual_window_frame=4、window_caption=4、其余 0
    3. pubspec.yaml：删 L33-34（注释行「模板 Dart 平台接口层…」+ `plugin_platform_interface: ^2.0.2`）；L30 白名单注释「三项」改「两项」；其余（身份五字段/sdk floor/topics/path/screen_retriever/plugin 段）一字不动
    4. test 面：cp 上游 test/window_manager_test.dart → test/（保留上游文件名）；sed 改 :3 注释 import + :6 channel 串（共 2 行）
    5. example Dart 面：cp main.dart / pages/home.dart（新建目录）/ utils/config.dart / integration_test/window_manager_test.dart / images 4 张；sed 三连（barrel 长串优先、`window_manager_example`→`window_frame_kit_example`、`window_manager_test`→`window_frame_kit_test`）。home.dart 上游 :1087 的 print 行**原样保留**（独立 logger 方案，home.dart 只 4 行重命名）
    6. 新建 example/lib/utils/window_event_logger.dart（新码区 ~55 行，RESEARCH Pattern 4）：`class WindowEventLogger with WindowListener`，15 个具名 hook + `onWindowEvent(String)` 各 debugPrint 一行 `[window_frame_kit] {ISO时间戳} {hook名/事件名}`；tall-style format 干净
    7. main.dart logger 注册 +2 行（Open Q2 定案=独立 logger）：import 'utils/window_event_logger.dart' + `windowManager.ensureInitialized()` 后 `windowManager.addListener(WindowEventLogger());`——main.dart 适配面 3→5 行，两行均含 `WindowEventLogger` 字样（Task 2 脚本按此滤除）
    8. example/pubspec.yaml：+bot_toast ^4.1.3 / preference_list ^0.0.2 / tray_manager ^0.5.3（cupertino_icons 保留骨架 ^1.0.8）+ `assets: - images/` 段
    9. `D:/flutter/bin/flutter pub get && (cd example && D:/flutter/bin/flutter pub get)`——三平台 registrant + generated_plugins.cmake + example/pubspec.lock 再生成，全部纳入本 commit（Pitfall 13）
    本任务执行 D-01（costly，落盘即锁）：不改任何类名/单例名/常量名。
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && D:/flutter/bin/flutter analyze && ! grep -rn "window_frame_kit/window_manager.dart" lib test example && [ "$(grep -rn window_manager lib/ | grep -v 'src/window_manager.dart' | grep -v 'window_caption.dart:19' | wc -l)" -eq 0 ] && [ "$(grep -c win32 pubspec.lock)" -eq 0 ] && [ "$(grep -c win32 example/pubspec.lock)" -eq 0 ] && ! grep -q plugin_platform_interface pubspec.yaml && grep -c TrayManagerPlugin example/windows/flutter/generated_plugin_registrant.cc</automated>
    <fails_when>analyze 输出任何 "error •"/"issues found"（barrel 404 import 表现为 "Error when reading ...window_manager.dart"=Pitfall 3 sed 顺序错）；`window_frame_kit/window_manager.dart` grep 非空（双重替换漏做）；lib 残留 grep 出白名单（src/window_manager.dart 文件名引用 + window_caption.dart:19 doc comment）之外的 window_manager 串；任一 lockfile grep win32 ≥1；pubspec 仍含 plugin_platform_interface；registrant 无 TrayManagerPlugin（pub get 未在 example 目录跑或生成件未再生）</fails_when>
  </verify>
  <done>模板五件删除、上游 Dart 面 17 文件落位、logger + 注册行就位、example 四依赖解析、registrant×3 再生成、analyze 0 issue、双 lock 零 win32</done>
  <acceptance_criteria>
    - lib/ = 1 barrel + 11 src（含 utils/1 + widgets/5）= 12 上游文件；模板 platform_interface/method_channel 已不存在
    - barrel lib/window_frame_kit.dart 与上游 lib/window_manager.dart 字节一致（diff 零差）
    - example/lib/pages/home.dart 改动恰 4 行（:10/:11/:703/:947）；main.dart 改动恰 5 行（3 重命名 + 2 logger）；config.dart 字节一致
    - grep -c "MethodChannel('window_frame_kit')" lib/src/window_manager.dart = 1
    - example/pubspec.yaml 含 bot_toast/preference_list/tray_manager 三行 + assets images/
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 证明体系落地——契约测试 + verbatim_diff.sh（A 段）+ ci.yml 三改 + DEVIATIONS 5 行 + WINDOWS.md 记账</name>
  <files>test/channel_contract_test.dart, tool/verbatim_diff.sh, .github/workflows/ci.yml, DEVIATIONS.md, .planning/WINDOWS.md</files>
  <read_first>
    - 02-RESEARCH.md §Code Examples Example 2（verbatim_diff.sh 全文）+ Example 3（契约测试骨架，probe 4/4 绿原型）+ Open Question 5（契约宽度定案=每方法组 1-2 代表，~12-15 用例）+ Pattern 2/3（双轨纪律 + 5 行 DEVIATIONS 清单）+ Pitfall 1（format 门双轨化，豁免清单 14/19 实测）+ Pitfall 2（exit 79 机制）
    - .github/workflows/ci.yml（现 L30 format 门、L44-48 ubuntu apt、pana 门勿动）
    - DEVIATIONS.md（六列格式 + 三条记账规则；行 1 placeholder 保留给 Phase 3）
    - lib/src/window_manager.dart:15-30（15 事件常量 kWindowEvent* 上游值）
  </read_first>
  <action>
    1. 新建 test/channel_contract_test.dart（新码区，tall-format 干净，~12-15 用例）：以 Example 3 的 4 个 probe 用例为核（channel 名锚点捕获 / 15 事件常量逐字 / handlePlatformMessage 注入驱动 15 具名 hook + raw onWindowEvent / getBounds devicePixelRatio 参数 + x/y/width/height 返回解析），按 Open Q5 定案扩每方法组代表断言：setMinimumSize{devicePixelRatio,width,height}、maximize{vertically}、startResizing{resizeEdge+4 bool}、setBackgroundColor{ARGB 4 键}、setTitleBarStyle{titleBarStyle,windowButtonVisibility}、setIgnoreMouseEvents{ignore,forward}、setPreventClose{isPreventClose}、popUpWindowMenu 捕获
    2. 新建 tool/verbatim_diff.sh（Example 2 全文为基底，本计划只落 A 段 Dart + main.dart 特例；B 段 C++ 由 02-02 追加）：17 对文件映射；`norm() { sed 's/window_frame_kit/window_manager/g' "$1"; }`；**main.dart 特例**：先滤除含 `window_event_logger`/`WindowEventLogger` 的行（大小写不敏感）再归一化 diff，滤除行数断言恰 2（否则 fail）；末尾输出 `VERBATIM PROOF OK (17 dart files zero-diff)`（B 段接入前不含 cpp 字样）
    3. ci.yml 三处改动（唯一 CI delta，pana 门/触发器/mkdir 预防步不动）：① L30 `dart format --set-exit-if-changed --output=none .` → 正名单 `dart format --set-exit-if-changed --output=none test/channel_contract_test.dart example/lib/utils/window_event_logger.dart example/test/widget_test.dart`（逐字区豁免，Pitfall 1）；② L48 apt 清单增 `libayatana-appindicator3-dev`（Pitfall 12/A1）；③ ubuntu leg 增 verbatim-diff 步：`mkdir -p /tmp/wm052 && curl -sL https://pub.dev/api/archives/window_manager-0.5.2.tar.gz | tar xz -C /tmp/wm052 && bash tool/verbatim_diff.sh /tmp/wm052`（Open Q3 定案=进 CI）
    4. DEVIATIONS.md 追加 5 行（Pattern 3 清单，六列格式，placeholder 行 1 保留）：①包身份机械重命名（anchor: 上游 lib/src/window_manager.dart:9-13,43 等，marker=n/a，脚本证明）②C-API 桥接块（anchor: window_manager_plugin.cpp:592-597，marker `// DEV:`，02-02 落码本行先记账）③window_manager.cpp:1 include 改径（行数记账 2，02-02 落码）④windows/CMakeLists 合并三处（anchor: 上游 CMakeLists:10-17 + 骨架 test 块，02-02 落码）⑤example 适配集（anchor: main.dart:3-5/home.dart:10-11,703,947/integration_test:6,14,118 + 事件日志新增 D-04 授权）
    5. .planning/WINDOWS.md 追加 known-stub 行：example/test/widget_test.dart 模板版在 main.dart 覆盖后 fail-if-run、CI 永不跑（root flutter test 不含嵌套包）、上游同款同样坏、Phase 5 清理（Open Q4 定案）
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2" && D:/flutter/bin/flutter test && D:/flutter/bin/dart format --set-exit-if-changed --output=none test/channel_contract_test.dart example/lib/utils/window_event_logger.dart example/test/widget_test.dart && grep -c libayatana-appindicator3-dev .github/workflows/ci.yml && grep -c verbatim_diff.sh .github/workflows/ci.yml</automated>
    <fails_when>脚本输出任何 "VERBATIM FAIL: <file>" 或 main.dart 滤除行数 ≠2（logger 注册行漂移）或退出码非 0；flutter test 退出码非 0 / 输出 "No tests were found."（Pitfall 2：契约测试未与模板删除同落）/ 通过数 <+4；format 正名单退出码非 0（新码区被写成 short-style）；ci.yml 两个 grep 计数为 0（apt 行或 diff 步漏改）；ci.yml 仍残留全仓 `--output=none .` 形式 format 门</fails_when>
  </verify>
  <done>契约测试 ≥12 绿（底线 4）、verbatim_diff.sh A 段对 pub cache 零差、format 正名单干净、ci.yml 三改落定、DEVIATIONS 5 行、WINDOWS.md stub 记账</done>
  <acceptance_criteria>
    - flutter test 输出形如 "+N: All tests passed!"，N ≥ 12（probe 底线 4，Open Q5 铺宽后应 ≥12；<4 即 Pitfall 2 红线）
    - tool/verbatim_diff.sh 输出 "VERBATIM PROOF OK (17 dart files zero-diff)"
    - test/channel_contract_test.dart 含 `MethodChannel('window_frame_kit')` 锚点断言与 15 条 kWindowEvent* 逐字断言（含 kWindowEventUndocked='undocked'）
    - DEVIATIONS.md ledger 表 = 1 placeholder + 5 新行；ci.yml pana 门 threshold 10 原样
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 原子 commit → 发布门 → push → CI 三 leg 绿确认</name>
  <files>（无新文件改动；git 操作 + 门复跑）</files>
  <read_first>
    - 02-RESEARCH.md Pitfall 13 + Phase 1 惯例（01-01-SUMMARY：dry-run 0-warnings 门要求先 commit 再跑）
    - 02-VALIDATION.md §Sampling Rate（per plan 收口 = full suite 本地半边 + CI 三 leg）
    - 02-RESEARCH.md Assumptions A1/A2/A3/A7（首推验证点与回退路径）
  </read_first>
  <action>
    1. 单 commit 收拢 Task 1+2 全部文件（含生成件与 example/pubspec.lock、DEVIATIONS/WINDOWS.md）：`feat(02-01): port window_manager 0.5.2 Dart surface verbatim + proof harness`
    2. commit 后本地跑发布门（dry-run 的 git-status 检查要求工作区干净——Phase 1 实测教训）
    3. push master；`gh run list -R TATboy210/window_frame_kit --workflow ci --limit 1` 盯三 leg + pana job（显式 -R，双 remote 坑）。A1/A2/A3/A7 首推验证点：ubuntu apt（appindicator）、macos tray_manager SwiftPM、pana 不降分、四依赖全平台解析——任一红按 RESEARCH 回退路径修：ubuntu 红→回退 `appindicator3-dev`（tray CMake 双回退链）；macos 红→按输出修（Flutter tool 自动 SwiftPM↔CocoaPods 回退）；pana 红→WINDOWS.md #5 流程重审
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && D:/flutter/bin/dart pub publish --dry-run && git remote get-url origin | grep -q "github.com[:/]TATboy210/window_frame_kit" && gh run list -R TATboy210/window_frame_kit --workflow ci --limit 1 --json conclusion --jq '.[0].conclusion' | grep -qx success</automated>
    <fails_when>dry-run 输出非 "Package has 0 warnings."（未提交生成件被 git-status 检查计 warning——Pitfall 13）；gh conclusion 为 failure/cancelled（读 job 明细定位 A1/A2/A3/A7 哪条假设破产）；git status 仍有未提交改动</fails_when>
  </verify>
  <done>单原子 commit 落盘、dry-run 0 warnings、push 后 CI run conclusion=success（windows/ubuntu/macos 三 leg + pana 全绿）</done>
  <acceptance_criteria>
    - git log -1 --stat 显示本 commit 同时含 lib/、test/、example/（含 registrant + pubspec.lock）、tool/、ci.yml、DEVIATIONS.md——零后续修补 commit
    - CI 四 job（checks×3 + pana）全绿；ubuntu leg verbatim-diff 步日志含 "VERBATIM PROOF OK"
    - `! grep -rn "FRAME:" lib windows example test tool` 通过（退出门条款在 Dart 面已成立）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub cache / pub.dev archive → 本仓库 | 移植源字节流进入逐字区（供应链完整性） |
| 开发者/IDE → 逐字区文件 | format-on-save 与"顺手修 quirk"是逐字性的唯一现实威胁 |
| example 新依赖 → 依赖树 | bot_toast/preference_list/tray_manager 进入 example lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | 逐字区 17 Dart 文件（IDE reformat / opportunistic edit） | high | mitigate | tool/verbatim_diff.sh 每 commit 本地 quick run + CI ubuntu leg verbatim-diff 步（curl 官方 archive 为对照源）——破坏即时红灯 |
| T-02-02 | Tampering | channel 名四锚（复用 'window_manager' 或锚点漂移） | medium | mitigate | Task 1 grep 门：Dart 常量=1、`MethodChannel('window_manager')` 零匹配；native 三锚 grep 沿用 Phase 1 命令 |
| T-02-03 | Spoofing | 宿主双包共存期 channel/类名冲突 | low | mitigate | D-01 类名全保真 + D-03 互斥换 import 纪律（同名类冲突结构性消除；channel 名两包不同） |
| T-02-04 | DoS | CI 流水线（Pitfall 2 exit 79 / Pitfall 12 appindicator FATAL_ERROR） | medium | mitigate | 契约测试与模板删除同 commit（fails_when 显式盯 "No tests were found."）；apt 行与 verbatim 步同 commit 落 ci.yml |
| T-02-05 | Tampering (supply chain) | example 四新依赖 | low | accept | RESEARCH §Package Legitimacy Audit：全部 OK/Approved，零 SLOP/SUS，双 lock 零 win32 门复跑；preference_list discontinued 为 example-only，Phase 5 评估 |
| T-02-06 | Information Disclosure | 运行期数据面 | low | accept | 移植阶段无新增运行期攻击面（无网络/凭据路径；setIcon 路径拼接为上游 verbatim） |
| T-02-07 | Elevation of Privilege | — | low | accept | 无提权面；preventClose 的 destroy 逃生门属 UX 安全，02-05 UAT 验证 |
</threat_model>

<verification>
Quick run（每 task commit）：`cd /d/window_frame_kit && D:/flutter/bin/flutter analyze && D:/flutter/bin/flutter test && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2"`
Plan 收口：+ format 正名单 + `dart pub publish --dry-run` + CI 三 leg 绿（本计划无 example build/启动门——windows C++ 面 02-02 才接通，headless 边界内全绿即收口）
</verification>

<success_criteria>
- SC5 Dart 半边成立：17 文件 sed 归一化零差（机器证明，CI 持续门）
- D-01/D-02/D-04 执行完毕：API 全保真、契约测试叠加层、example+logger 验证载体就位
- F-3 白名单收敛：pubspec 仅 path + screen_retriever
- CAPB-01~12 的 Dart facade + headless 契约证明全部落位（native 半边在 02-02/03/04/05）
- CI 三 leg + pana 绿，dry-run 0 warnings
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (本计划)
- lib/window_frame_kit.dart（barrel，字节=上游）+ lib/src/**（11 文件逐字区）
- test/window_manager_test.dart（上游壳）+ test/channel_contract_test.dart（新码区 ~12-15 用例）
- example/lib/{main.dart,pages/home.dart,utils/config.dart,utils/window_event_logger.dart} + example/integration_test/window_manager_test.dart + example/images/*4
- tool/verbatim_diff.sh（A 段）· ci.yml 三改 · DEVIATIONS.md 5 行 · WINDOWS.md known-stub 行
</artifacts_produced>

<output>
完成后创建 `.planning/phases/02-windows/02-01-SUMMARY.md`
</output>
