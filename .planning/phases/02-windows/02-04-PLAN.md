---
phase: 02-windows
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - .planning/phases/02-windows/02-UAT-EVIDENCE.md   # 追加批② 段
  - .planning/WINDOWS.md                              # 仅在发现回归/新开放项时追加行
autonomous: false
requirements: [CAPB-04, CAPB-05, CAPB-06]
estimate:
  tokens: 22000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SC3 实机签核：11 个 WindowListener hook（close/focus/blur/maximize/unmaximize/minimize/restore/resize/move/enter-full-screen/leave-full-screen）在对应窗口操作时控制台逐项出行（Windows 可触发具名事件共 13 = 11 必验 + resized/moved；show/hide 走 raw onWindowEvent；docked/undocked Windows 不发射 = 上游 parity 非缺陷）"
    - "SC2 实机签核：maximize/unmaximize/minimize/restore/setFullScreen 状态操作与 isFullScreen/isMaximized/isMinimized/isFocused 等查询一一对应（每操作后对应查询回读一致）"
    - "7 个 Windows-NotImplemented 方法（isMovable/setMovable/isVisibleOnAllWorkspaces/setVisibleOnAllWorkspaces/setBadgeLabel/grabKeyboard/ungrabKeyboard）在 example 点击报 MissingPluginException 记为预期（plugin.cpp:585-587 上游行为），不当回归处理"
    - "logger 存活前置证：example 启动后 focus 事件行即时出现（02-02 tracer 路径复通，本批 UAT 的观测仪器就绪）"
  artifacts:
    - .planning/phases/02-windows/02-UAT-EVIDENCE.md 批② 段（RESEARCH Example 5 触发-事件对照表逐项判定 + 查询交叉回读表 + dated sign-off）
  key_links:
    - "窗口操作 → native _EmitEvent（plugin.cpp:206-215 focus/blur、:302-316 状态机、:226-232 move/resize、:292-300 fullscreen）→ channel onEvent → lib/src/window_manager.dart funcMap 分发 → WindowEventLogger 控制台行"
    - "契约测试（02-01 已绿）证 Dart 分发半边 ↔ 本批实机证 native 发射半边 = SC3 两半合拢"
  prohibitions:
    - statement: "零嫁接码/禁改区：事件发射逻辑（plugin.cpp 全部 _EmitEvent 调用点）与 WM_SIZE 状态机 verbatim——某 hook 不触发时不得改 native 发射条件来'修绿'，先判移植回归还是上游 parity"
      status: resolved
      verification: "! grep -rn 'FRAME:' lib windows example test 且 verbatim_diff.sh 绿（22/2 不漂移）"
    - statement: "docked/undocked 在 Windows 缺席 = 预期（常量仅 Dart 侧，native 无发射点，RESEARCH CAPB-06 行枚举核实）——不得为'补全'而新增 native 发射码（新增=偏离上游）"
      status: resolved
      verification: "UAT-EVIDENCE 批② 段该两行标注 expected-absent；windows/ 无 diff"
    - statement: "minimize/close 类操作不进任何 CI/自动化脚本（Pitfall 11 挂起 runner 风险）——本批全部实机项人工触发"
      status: resolved
      verification: "git diff 不含 .github/workflows/ci.yml 与 integration_test 新增"
    - statement: "上游 quirk 不修：NotImplemented 7 方法、setFullScreen 的 is_frameless_ 分支（cpp:593）等全部 verbatim；预期值按上游语义写判定表（Pitfall 9 纪律）"
      status: resolved
      verification: "判定表预期列引用 RESEARCH file:line；quick run 收口绿"
---

<objective>
D-05 批② 实机对等验证：CAPB-04（状态查询全套）/ CAPB-05（状态操作全套）/ CAPB-06（WindowListener 11 hook 事件流）。核心是 SC3 的两半合拢——Dart 分发半边已由契约测试 headless 证明（02-01，probe 4/4），本批用 RESEARCH Example 5 触发-事件对照表在实机证明 native 发射半边：逐项操作窗口、控制台逐项出行打勾。

**用户故事（planner 按 ROADMAP Goal 框定）：** 作为包作者，我要 11 个事件 hook 与状态查询/操作在实机与上游行为一一对应，以便宿主（simple_player_flutter 依赖事件流做窗口态协调）在 Phase 6 替换时零行为意外。

**Purpose:** SC2（状态半边）+ SC3 全项的实机判定；退出门四组 UAT 的第二组。
**Output:** UAT-EVIDENCE 批② 段 sign-off；commit「test(02-04): batch② parity UAT — state ops/queries + 11 listener hooks (CAPB-04/05/06)」。
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-windows/02-CONTEXT.md
@.planning/phases/02-windows/02-RESEARCH.md
@.planning/phases/02-windows/02-VALIDATION.md
@.planning/phases/02-windows/02-UAT-EVIDENCE.md
@.planning/phases/02-windows/02-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 观测仪器就绪证——logger 事件行存活冒烟 + Example 5 对照表誊入账本</name>
  <files>.planning/phases/02-windows/02-UAT-EVIDENCE.md（批② 段开卷：对照表 + 查询交叉回读表骨架）</files>
  <read_first>
    - 02-RESEARCH.md Example 5（触发-事件对照表全文，含 native 发射点 file:line 与 docked/undocked 预期缺席行）+ CAPB-04 行（7 个 NotImplemented 方法清单）+ CAPB-05 行（setSkipTaskbar/setProgressBar 的 taskbar_ 时序前提，Pitfall 6）
    - example/lib/utils/window_event_logger.dart（日志行格式 = 打勾判读基准）
    - example/lib/pages/home.dart（状态操作/查询条目的 UI 位置）
  </read_first>
  <action>
    1. 把 Example 5 对照表誊入 UAT-EVIDENCE 批② 段作判定表（列：UAT 操作 / 预期控制台行 / native 发射点 / 用户判定 / 日期），并加三行专表：show/hide（raw onWindowEvent 无具名 hook = 上游如此）、docked/undocked（expected-absent = parity）、NotImplemented 7 方法（点击报 MissingPluginException = 预期）
    2. 加"查询交叉回读表"骨架（操作 → 对应查询：maximize→isMaximized=true、setFullScreen(true)→isFullScreen=true、minimize→isMinimized=true、restore 后三查询复位、focus/blur→isFocused）
    3. 冒烟：后台拉起 example exe，数秒后断言进程存活（logger 注册路径与 02-02 tracer 同链——进程活着 + Task 2 用户首个 focus 行即仪器就绪证），随后留给 Task 2 用 `flutter run` 正式跑（此步只做快速存活，不做目视）
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && grep -q "批②" .planning/phases/02-windows/02-UAT-EVIDENCE.md && grep -c "expected-absent" .planning/phases/02-windows/02-UAT-EVIDENCE.md && grep -c "MissingPluginException" .planning/phases/02-windows/02-UAT-EVIDENCE.md</automated>
    <fails_when>账本无批② 段；expected-absent 计数 0（docked/undocked 预期缺席行漏誊——UAT 时会把 parity 误判为缺陷，Pitfall 9 的 Warning sign）；MissingPluginException 计数 0（NotImplemented 7 方法预期行漏誊）</fails_when>
  </verify>
  <done>批② 判定表 + 交叉回读表 + 三行预期专表落账；exe 存活冒烟通过</done>
  <acceptance_criteria>
    - 判定表行数 ≥ Example 5 的 9 行 + 3 行专表；每行含 native 发射点 file:line
    - 交叉回读表含 isMaximized/isMinimized/isFullScreen/isFocused 四查询
    - 无代码改动（git status --porcelain 仅 .planning/**）
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 2: 批② 实机 UAT checkpoint——SC3 事件流 11 hook + SC2 状态操作/查询一一对应（CAPB-04/05/06）</name>
  <files>.planning/phases/02-windows/02-UAT-EVIDENCE.md（批② 判定 + sign-off）, .planning/WINDOWS.md（仅新开放项时）</files>
  <read_first>
    - 02-VALIDATION.md §Manual-Only Verifications 行 4-5（状态↔查询 / 11 hooks）
    - 02-RESEARCH.md Pitfall 6（UAT 引导纪律：不裸调 setSkipTaskbar/setProgressBar——必须先经 waitUntilReadyToShow 的规范序列，example 启动路径已保证）、Pitfall 7（setAsFrameless 必须排在 waitUntilReadyToShow 之后——本批全屏项操作顺序基准）
    - .planning/phases/02-windows/02-UAT-EVIDENCE.md（Task 1 落好的判定表——照表逐项走）
  </read_first>
  <action>
    executor 拉起 `cd /d/window_frame_kit/example && D:/flutter/bin/flutter run -d windows`（控制台朝向用户），照账本判定表逐项引导：每项 = 用户操作窗口/点 example 条目 → 读控制台行 → 回答"是/否+实际所见"；executor 逐项记账。
  </action>
  <human_check>
    **判定清单（照 Example 5 誊入的判定表逐项，核心组如下）：**
    1. **focus/blur：** 点窗口/点别处 → 控制台 focus / blur 行
    2. **maximize/unmaximize：** 点最大化按钮（或 example 条目）→ maximize 行 + isMaximized 回读 true；还原 → unmaximize 行 + 回读 false
    3. **minimize/restore：** 最小化 → minimize 行 + isMinimized true；还原 → restore 行 + false
    4. **resize/resized + move/moved：** 拖边框中连续 resize 行、松手恰一次 resized；拖动窗口同理 move/moved
    5. **enter-full-screen/leave-full-screen：** setFullScreen(true/false) → 对应行 + isFullScreen 回读一致（frameless 分支不涉及本批——is_frameless_ 默认 false，Phase 3 才碰）
    6. **close：** 点关闭按钮 → close 行（preventClose 拦截流的完整验证在批③/SC4，此处只证事件行出现且 example 对话框弹出）
    7. **show/hide 专行：** example 触发 show()/hide() → 控制台 raw onWindowEvent 行（无具名 hook = 上游语义，判"符合预期"而非缺陷）
    8. **docked/undocked 专行：** 确认控制台全程无此两行 → 判 expected-absent（parity）
    9. **NotImplemented 7 方法专行：** 逐一点击 example 对应条目 → 报 MissingPluginException（控制台/对话框可见）判预期；记录报错形态
    10. **状态操作余量（CAPB-05）：** setResizable(false) 后拖边框无效 + isResizable 回读 false；setSkipTaskbar(true) 任务栏图标消失（规范序列已保证 taskbar_ 初始化——Pitfall 6 前提成立才操作）；setAlwaysOnTop 置其他窗口下验证（该项与批③ 外观组共享则记账指向批③）
    **通过标准：** 1-10 全"符合预期"（含三个专行的预期缺席/预期报错）→ 批② dated sign-off。
    **响应协议：** 用户回复 "approved"/逐项确认 → 记账收口 commit；任何 hook 不出行或查询回读不一致 → 记录症状原文 + 操作序列，先对照判定表 native 发射点 file:line 与契约测试（Dart 半边已绿）定位断在 native 发射还是分发，判移植回归才修（修复过 quick run + verbatim_diff + DEVIATIONS 记账后**本批复验**）；把 parity 项（docked 缺席/NotImplemented/物理 px）报成缺陷 = Pitfall 9 Warning sign，先对照宿主 window_manager 现行为再判。
  </human_check>
  <verify>
    <automated>cd /d/window_frame_kit && grep -A80 "批②" .planning/phases/02-windows/02-UAT-EVIDENCE.md | grep -q "approved" && D:/flutter/bin/flutter analyze && D:/flutter/bin/flutter test && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2"</automated>
    <fails_when>账本批② 段无 "approved"（未签核即收口）；quick run 任何一门红（本批若产生修复，17 文件零差 + 22/2 + 契约 ≥12 绿必须仍成立）；判定表存在空判定行（漏项）</fails_when>
  </verify>
  <done>批② 判定表全行落判 + dated sign-off + quick run 绿 + commit（账本文件）</done>
  <acceptance_criteria>
    - SC3 的 11 必验 hook 全部有用户确认行；resized/moved 附加行确认；三个专行按预期语义落判
    - 交叉回读表四查询（isMaximized/isMinimized/isFullScreen/isFocused）全一致
    - CAPB-04/05/06 在批② 段标记实机验证通过；commit 仅含 .planning/**（无回归情形）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native 事件发射 → Dart 分发 → 控制台观测 | SC3 证据链跨三层，断点定位须可分辨 |
| 上游 parity 行为 ↔ 本包缺陷 | 误归类会导致无谓修复循环（Pitfall 9）或漏修真回归 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Tampering | 为"修绿"某 hook 而改 native 发射条件/WM_SIZE 状态机（逐字区违规） | high | mitigate | prohibitions 显式禁改发射逻辑；verbatim_diff 22/2 收口门兜底；修复必须 DEVIATIONS 记账 |
| T-02-19 | Repudiation | 11 hook 判定无逐项证据（"应该都触发了"） | medium | mitigate | 判定表逐行含用户判定 + 日期；fails_when 盯空判定行与 approved 字样 |
| T-02-20 | Spoofing | parity 行为冒充回归（docked 缺席 / NotImplemented / raw show-hide）被误修 | medium | mitigate | Task 1 预誊三行预期专表（判定前锁定预期语义）；响应协议强制先对照宿主 WM 现行为 |
| T-02-21 | DoS | minimize/close 类自动化挂起 runner（Pitfall 11） | medium | mitigate | 本批零 CI/脚本化改动（prohibitions + files_modified 界定）；全部人工触发 |
</threat_model>

<verification>
Quick run（每 commit）：`cd /d/window_frame_kit && D:/flutter/bin/flutter analyze && D:/flutter/bin/flutter test && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2"`
Plan 收口：批② 判定表全落 + sign-off + quick run 绿
</verification>

<success_criteria>
- SC3 全项实机签核：11 hook 逐项控制台出行（13 可触发 + show/hide raw + docked/undocked 预期缺席）
- SC2 状态半边实机签核：操作 ↔ 查询交叉回读一一对应；NotImplemented 7 方法按预期记录
- SC3 两半合拢成立：契约测试（Dart 分发）+ 本批（native 发射）
- 零代码回归；逐字性 22/2 + 17 文件零差不漂移
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (本计划)
- UAT-EVIDENCE 批② 段（Example 5 判定表 + 交叉回读表 + 三预期专行 + sign-off）
- （条件产物）WINDOWS.md 开放项 / DEVIATIONS.md 回归修复行
</artifacts_produced>

<output>
完成后创建 `.planning/phases/02-windows/02-04-SUMMARY.md`
</output>
