---
phase: 02-windows
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - .planning/phases/02-windows/02-UAT-EVIDENCE.md   # 新建：三批共用的 UAT 证据账本（本计划开卷，02-04/02-05 追加）
  - .planning/WINDOWS.md                              # 仅在发现回归/新开放项时追加行
autonomous: false
requirements: [CAPB-01, CAPB-02, CAPB-03]
estimate:
  tokens: 25000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "本地实机 integration_test 绿：example/integration_test/window_manager_test.dart 17 个 testWidgets 在 Windows 实机通过（getSize==640×480 / getTitle=='window_frame_kit_test' / getBounds isA<Rect> 等精确断言；上游按平台 skip 的用例照 skip）——CI 永不挂此测试（Pitfall 11 挂起风险）"
    - "批① UAT dated sign-off：SC1 全项 + CAPB-02/03 目视项逐条通过（WindowOptions 逐字段、几何读写即时生效、100%+150% DPI 逻辑像素换算、min/max 拖拽约束、aspectRatio 比例锁定）"
    - "任何红项走诊断路径而非直接判回归：DPR 链路（lib/src/window_manager.dart:97-101 → window_manager.cpp:718-776）先核，A5 风险条款"
  artifacts:
    - .planning/phases/02-windows/02-UAT-EVIDENCE.md（批① 段：逐项判定 + dated sign-off + integration_test 实跑输出摘要）
  key_links:
    - "integration_test waitUntilReadyToShow(640×480,title) → native GetBounds ÷ devicePixelRatio → Dart 逻辑像素断言（CAPB-02 换算链全通）"
    - "setMinimumSize/setMaximumSize 逻辑 px + pixel_ratio_ 存值（window_manager.cpp:778-806）→ WM_GETMINMAXINFO 条件覆盖（plugin.cpp:190-205，禁改区 verbatim）→ 拖拽被挡"
  prohibitions:
    - statement: "零嫁接码 / 禁改区：发现 min/max 行为不符时不得改 WM_GETMINMAXINFO（plugin.cpp:190-205）——那是 Phase 3 协作式合并的战场；本批只验证上游对等行为并记账"
      status: resolved
      verification: "! grep -rn 'FRAME:' lib windows example test 且 verbatim_diff.sh 全程绿（22/2 不漂移）"
    - statement: "不修上游 quirk：getTitleBarHeight 物理 px / DPI 换算走上游 devicePixelRatio 链路 verbatim——UAT 预期值按上游语义写（Pitfall 9），'不符理想'不是回归"
      status: resolved
      verification: "UAT-EVIDENCE 批① 段的预期值列引用 RESEARCH Example 5 / Pitfall 9 语义而非理想值"
    - statement: "不在 CI 跑 integration_test（minimize/hide/close 类操作有挂起 runner 风险，Pitfall 11）；不新增 CI 步骤"
      status: resolved
      verification: "git diff 不含 .github/workflows/ci.yml"
    - statement: "零新增 UI/新码：本批验证不产生任何 lib/example 代码改动（发现回归 → 修复须记 DEVIATIONS 并复跑 quick run，且不得触碰逐字区语义）"
      status: resolved
      verification: "git status --porcelain 仅 .planning/**（无回归时）"
---

<objective>
D-05 批① 实机对等验证：CAPB-01（初始化协议）/ CAPB-02（几何读写 + 逻辑像素换算）/ CAPB-03（min/max/aspectRatio）。自动化半边 = 上游 integration_test 本地实机跑（现成 17 用例精确断言）；人工半边 = SC1 + 几何/约束目视清单，用户 dated sign-off。

**用户故事（planner 按 ROADMAP Goal 框定）：** 作为包作者，我要在实机上证明几何读写与 DPI 换算和上游 window_manager 行为对等，以便 Phase 3 嫁接后任何几何异常都能被判定为嫁接回归而非移植回归。

**Purpose:** SC1/SC2（几何半边）的实机判定；退出门四组 UAT 的第一组。
**Output:** UAT-EVIDENCE 批① 段 sign-off + integration_test 本地绿记录；commit「test(02-03): batch① parity UAT — init protocol + geometry + min/max (CAPB-01/02/03)」。
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-windows/02-CONTEXT.md
@.planning/phases/02-windows/02-RESEARCH.md
@.planning/phases/02-windows/02-VALIDATION.md
@.planning/phases/02-windows/02-02-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 本地实机 integration_test 全量跑（几何/初始化精确断言的自动化半边）</name>
  <files>.planning/phases/02-windows/02-UAT-EVIDENCE.md（开卷：integration_test 实跑记录段）</files>
  <read_first>
    - example/integration_test/window_manager_test.dart（17 testWidgets 全文：waitUntilReadyToShow 前置段 / getSize==640×480 / getTitle / getBounds / setBounds / 查询断言与平台 skip 表）
    - 02-RESEARCH.md CAPB-02 行（GetBounds ÷DPR cpp:718-740、SetBounds ×DPR cpp:742-776、DPR 来源 src/window_manager.dart:97-101）+ Assumption A5（红时诊断路径）+ Pitfall 11（本地跑不进 CI 的理由）
    - 02-VALIDATION.md §Manual-Only Verifications（本批人工项清单的权威源）
  </read_first>
  <action>
    1. 新建 02-UAT-EVIDENCE.md 账本骨架（三段：批①/批②/批③ + phase gate 段；每段 = 判定表 + sign-off 行）
    2. 实机跑：`cd /d/window_frame_kit/example && D:/flutter/bin/flutter test integration_test -d windows`（Windows 桌面设备在线；跑动中窗口会真实变化属预期）
    3. 结果记账：通过数/skip 数/失败数 + 失败用例名逐一摘录进账本
    4. 若有红：先走 A5 诊断路径——核 DPR 链路（src/window_manager.dart:97-101 传参 + cpp:718-776 乘除）与实机显示缩放（100%/150%），区分"上游语义 vs 移植回归 vs 环境（多显示器/DPI 切换）"；判定为移植回归才修（修复必须过 quick run + verbatim_diff 且记 DEVIATIONS）；判定为上游/环境行为则记账为预期并继续
  </action>
  <verify>
    <automated>cd /d/window_frame_kit/example && D:/flutter/bin/flutter test integration_test -d windows ; rc=$? ; cd /d/window_frame_kit && grep -q "integration_test" .planning/phases/02-windows/02-UAT-EVIDENCE.md ; test $rc -eq 0</automated>
    <fails_when>退出码非 0 且账本无对应红项诊断记录（红项必须先记账归类再收口，不得静默重跑到绿）；输出含 "No tests were found"（integration_test 目录/设备参数错）；账本缺 integration_test 段</fails_when>
  </verify>
  <done>integration_test 实机跑完毕（全绿，或红项已诊断归类记账）；UAT-EVIDENCE 开卷并含实跑输出摘要</done>
  <acceptance_criteria>
    - 账本记录：命令、日期、通过/skip/失败计数、失败用例诊断结论（若有）
    - 全绿情形：getSize==640×480 与 getTitle=='window_frame_kit_test' 精确断言在通过列表内（SC1/SC2 几何半边的机器证据）
    - 无代码改动（git status --porcelain 仅 .planning/**）；若有修复 commit，DEVIATIONS 有对应新行且 verbatim_diff.sh 绿
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 2: 批① 实机 UAT checkpoint——SC1 + 几何读写 + DPI 换算 + min/max/aspect（CAPB-01/02/03）</name>
  <files>.planning/phases/02-windows/02-UAT-EVIDENCE.md（批① 判定表 + sign-off）, .planning/WINDOWS.md（仅新开放项时）</files>
  <read_first>
    - 02-VALIDATION.md §Manual-Only Verifications 行 1-3（WindowOptions 出现 / DPI 双档换算 / min-max-aspect 拖拽）
    - 02-RESEARCH.md CAPB-01/02/03 行 + Example 5（resize/move 事件对照，UAT 时控制台旁证）
    - example/lib/pages/home.dart（几何/min-max 操作入口的 UI 位置——引导用户点哪里）
  </read_first>
  <action>
    executor 拉起 `cd /d/window_frame_kit/example && D:/flutter/bin/flutter run -d windows`（控制台可见），按下方判定清单逐项引导用户操作并记录回答；每项预期值按上游语义（Pitfall 9 纪律）。
  </action>
  <human_check>
    **判定清单（逐项操作 → 用户目视/回读确认）：**
    1. **初始化协议（CAPB-01/SC1）：** 冷启动后窗口按 main.dart WindowOptions 出现（尺寸/居中/标题栏样式对照源码读数）——02-02 tracer 已初证，此处复确认并记录
    2. **几何读写（CAPB-02/SC1）：** 在 example UI 触发 setSize(400,400)/setPosition/getSize 回读——窗口即时变化且控制台/回读值 = 设定值（逻辑像素）
    3. **DPI 双档（CAPB-02/SC1）：** 系统显示缩放切 100% 与 150% 各跑一次第 2 项——setSize(400,400) 后目视 + getSize 回读均为 400（物理尺寸随 DPI 变、逻辑值恒定）
    4. **center/setAlignment（CAPB-02）：** 触发 center——窗口目视居中
    5. **min/max（CAPB-03）：** setMinimumSize 后拖边框缩小——拖至 min 以下被挡；setMaximumSize 同理；reset（0,0）后解锁
    6. **aspectRatio（CAPB-03）：** setAspectRatio(16/9) 后拖边——比例锁定；reset 解锁
    7. **事件旁证：** 上述拖拽期间控制台连续 resize 行 + 松手一次 resized 行（Example 5 对照，native 发射点 plugin.cpp:230-232/216-219）
    **通过标准：** 7 项全过 → 批① dated sign-off 写入 UAT-EVIDENCE。
    **响应协议：** 用户回复 "approved"（或逐项"是"）→ 记账收口并 commit；任何一项异常 → 记录症状原文 + 当时的 DPI/显示器配置，按 Task 1 第 4 步诊断路径归类（上游语义/环境/移植回归），移植回归修复后**本批复验**再请用户签核；不得以"控制台无崩溃"替代目视判定（宿主 UAT 证据标准）。
  </human_check>
  <verify>
    <automated>cd /d/window_frame_kit && grep -q "批①" .planning/phases/02-windows/02-UAT-EVIDENCE.md && grep -A30 "批①" .planning/phases/02-windows/02-UAT-EVIDENCE.md | grep -q "approved" && D:/flutter/bin/flutter analyze && D:/flutter/bin/flutter test && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2"</automated>
    <fails_when>账本批① 段无 "approved"（未签核即收口）；quick run 任何一门红（本批若产生修复，逐字性/契约必须仍成立——22/2 与 17 文件零差不得漂移）</fails_when>
  </verify>
  <done>批① 7 项判定全过 + dated sign-off 落账 + quick run 绿 + commit（账本文件）</done>
  <acceptance_criteria>
    - UAT-EVIDENCE 批① 段：7 行判定表（项/预期上游语义/用户回答/日期）+ sign-off 行含 "approved" 与日期
    - CAPB-01/02/03 在该段标记为实机验证通过（SC1 全项 + SC2 几何/min-max 半边）
    - commit 仅含 .planning/**（无回归情形）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 实机环境（DPI/多显示器）→ 断言结果 | 环境差异可能被误判为移植回归（或反向：回归被误判为环境噪音） |
| UAT 判定 → phase 账本 | 无签核的"跑过了"不构成证据（Repudiation） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Repudiation | 批① UAT 结果无据/被事后追记 | medium | mitigate | UAT-EVIDENCE 账本强制 dated sign-off；checkpoint fails_when 盯 "approved" 字样 |
| T-02-15 | Tampering | 红项被"重跑到绿"或改断言迁就（silent failure） | high | mitigate | Task 1 fails_when：退出非 0 必须先记账诊断；修断言=逐字区违规（verbatim_diff 红灯兜底） |
| T-02-16 | DoS | integration_test 误入 CI 挂起 runner（Pitfall 11） | medium | mitigate | prohibitions 显式禁 ci.yml 改动；本计划 files_modified 不含 CI |
| T-02-17 | Spoofing | 环境行为冒充上游对等行为（误归类） | low | accept | A5 诊断路径 + 预期值按 RESEARCH 上游语义写死在判定表（Pitfall 9 纪律）；存疑项记 WINDOWS.md 开放账本 |
</threat_model>

<verification>
Quick run（每 commit）：`cd /d/window_frame_kit && D:/flutter/bin/flutter analyze && D:/flutter/bin/flutter test && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2"`
Plan 收口：integration_test 本地绿记录 + 批① sign-off + quick run 绿（无 CI 新增门——本批不 push 代码改动则沿用上次绿 run）
</verification>

<success_criteria>
- SC1 全项实机签核（初始化协议 + 几何即时生效 + DPI 双档逻辑像素换算）
- CAPB-03 拖拽约束实机签核（min/max 被挡 + aspect 锁定 + reset 解锁）
- integration_test 17 用例本地实机结果落账（全绿或红项归类清楚）
- 零代码回归；逐字性 22/2 + 17 文件零差不漂移
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (本计划)
- .planning/phases/02-windows/02-UAT-EVIDENCE.md（三批共用账本，本计划开卷 + 批① 段 sign-off）
- （条件产物）WINDOWS.md 新开放项行 / DEVIATIONS.md 回归修复行
</artifacts_produced>

<output>
完成后创建 `.planning/phases/02-windows/02-03-SUMMARY.md`
</output>
