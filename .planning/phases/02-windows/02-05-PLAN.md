---
phase: 02-windows
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - .planning/phases/02-windows/02-UAT-EVIDENCE.md   # 追加批③ 段 + phase gate 段
  - .planning/WINDOWS.md                              # 收口记账（开放项状态更新）
autonomous: false
requirements: [CAPB-07, CAPB-08, CAPB-09, CAPB-10, CAPB-11, CAPB-12]
estimate:
  tokens: 32000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "SC4 实机签核：setPreventClose(true) 后点关闭按钮窗口不消失（close 事件行 + example 对话框），确认路径显式 destroy 成功退出；popUpWindowMenu 在 frameless 下可唤出系统菜单（移动/大小/最小化项可点）"
    - "批③ 全项实机签核：titleBarStyle normal↔hidden 目视切换 + getTitleBarHeight 控制台值>0（物理 px 语义，150% DPI 下 ≈47 属上游行为 Pitfall 9）；startDragging 实拖 + DragToResizeArea 8 向实拖（fullscreen 下 Dart 守卫拒发=预期）；外观 9 项逐项目视；setIgnoreMouseEvents 穿透生效且 tray 菜单恢复路径可用（forward 参数无效=上游预期）"
    - "SC5 终跑全自动绿：verbatim_diff.sh 全量（17 Dart 文件零差 + cpp residue 22+2）+ 契约测试全绿 + DEVIATIONS 行数对账（1 placeholder + 5 新行 + 任何回归修复行，与脚本豁免一一对应）"
    - "Phase 退出门成立：SC1-4 四组全量复验签核 + CI 绿 + 零 `// FRAME:` grep + linux/macos channel 锚点未动——对等验证通过，Phase 3 嫁接解锁"
  artifacts:
    - UAT-EVIDENCE 批③ 段 + phase gate 段（SC1-4 复验矩阵 + SC5 终跑输出 + 退出门 checklist + 最终 dated sign-off）
    - WINDOWS.md 收口行（本 phase 开放项状态：#4 resize 基线观察留 Phase 3 勿动、#5 pana 留 Phase 5、known-stub 行确认）
  key_links:
    - "preventClose 语义链：WM_CLOSE → emit close → IsPreventClose 则 return -1（plugin.cpp:319-323）；destroy = PostQuitMessage(0)（cpp:232-234）绕过 WM_CLOSE——SC4 判定的语义基准（Pitfall 10）"
    - "穿透恢复链：setIgnoreMouseEvents(true) → WS_EX_TRANSPARENT|WS_EX_LAYERED（cpp:1058-1069）→ 窗口不可点击 → tray 菜单（home.dart:1043-1047）是唯一恢复路径——tray_manager 依赖保留的硬理由在 UAT 兑现"
    - "frameless 菜单链：setAsFrameless（须在 waitUntilReadyToShow 之后，Pitfall 7）→ is_frameless_ + WS_SYSMENU 保留 → popUpWindowMenu = GetSystemMenu + TrackPopupMenu at 光标位（cpp:1071-1089）"
    - "退出门 → ROADMAP Phase 3 依赖条款：'对等验证通过之前不得开始任何嫁接'由本计划 sign-off 解除"
  prohibitions:
    - statement: "零嫁接码（phase 退出门硬断言）：本 phase 全程不得出现任何 NCCALCSIZE/WM_NCHITTEST/GETMINMAXINFO 修改或 `// FRAME:` 标记"
      status: resolved
      verification: "! grep -rn 'FRAME:' lib windows example test tool（phase gate 终跑项）"
    - statement: "channel 名红线：四锚终验保持 window_frame_kit（Dart lib/src/window_manager.dart + windows plugin.cpp + linux plugin.cc:69 + macOS Swift:6——linux/macos 模板壳全程未动）"
      status: resolved
      verification: "四锚 grep 计数各 ≥1 且 linux/macos 文件相对 Phase 1 基线零 diff"
    - statement: "零 win32 Dart 依赖 + CMake 承重墙行保留（终验复核，全 phase 未漂移）"
      status: resolved
      verification: "grep -c win32 pubspec.lock example/pubspec.lock = 0/0；grep -c 'CMAKE_POLICY_VERSION_MINIMUM 3.5' windows/CMakeLists.txt = 1"
    - statement: "逐字纪律终态：lib/ + windows/ 移植面与上游 0.5.2 的偏离 = DEVIATIONS.md 5 行记账集，零未记账偏离；本批 UAT 发现的任何'想修的上游 quirk'（getTitleBarHeight 物理 px / forward 忽略 / IsWindows11OrGreater 恒真）一律不修，记 WINDOWS.md 留 Phase 3+/上游 issue"
      status: resolved
      verification: "verbatim_diff.sh 全量绿 + DEVIATIONS 行数对账 + WINDOWS.md 新开放项行（若 UAT 触发）"
    - statement: "lib/ 保持 verbatim：UAT 期间任何辅助脚本/日志改动只落 example 侧或 .planning/（D-04 零新增 UI）"
      status: resolved
      verification: "phase gate 段 git log 审查：批③ 期间 lib/ 零 commit 触碰（回归修复除外且已记账）"
---

<objective>
D-05 批③ 实机对等验证（CAPB-07 关闭拦截 / CAPB-08 标题栏样式 / CAPB-09 拖拽发起 / CAPB-10 外观置顶 / CAPB-11 鼠标穿透 / CAPB-12 系统菜单）+ **phase 退出门**：SC5 机器终跑 → SC4 与批③ 全量 UAT → SC1-4 复验矩阵签核 → CI 绿 + 零 FRAME grep。退出门条款（ROADMAP：对等验证通过之前不得开始任何嫁接）由本计划的最终 sign-off 解除，Phase 3 解锁。

**用户故事（planner 按 ROADMAP Goal 框定）：** 作为包作者与唯一用户，我要拿到"window_frame_kit 在 Windows 上与 window_manager 0.5.2 行为对等"的完整签核证据包，以便 Phase 3 的 frame 嫁接落在可分辨回归的已证基座上。

**Purpose:** SC4 全项 + SC5 终跑 + SC1-4 复验 = phase gate。
**Output:** UAT-EVIDENCE phase gate 段最终 sign-off；commit「test(02-05): batch③ UAT + phase gate — SC1-5 exit proof (CAPB-07~12)」。
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-windows/02-CONTEXT.md
@.planning/phases/02-windows/02-RESEARCH.md
@.planning/phases/02-windows/02-VALIDATION.md
@.planning/phases/02-windows/02-UAT-EVIDENCE.md
@.planning/phases/02-windows/02-04-SUMMARY.md
@DEVIATIONS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: SC5 机器门终跑 + 全 phase 不变量复核（自动化收口，先于人工批③）</name>
  <files>.planning/phases/02-windows/02-UAT-EVIDENCE.md（phase gate 段开卷：SC5 终跑输出 + 不变量 checklist）</files>
  <read_first>
    - 02-RESEARCH.md §Validation Architecture Sampling Rate 行 "Phase gate（P05 末）"（终跑项全集）+ Pattern 3（DEVIATIONS 5 行清单 = 对账基准）
    - DEVIATIONS.md（当前账面：1 placeholder + 5 新行 + 02-03/04 若有回归修复行）
    - tool/verbatim_diff.sh（A+B 段全文——终跑即全量）
    - .planning/phases/01-package-skeleton-license-foundation/01-01-SUMMARY.md（channel 四锚锚点行号基准）
  </read_first>
  <action>
    1. 跑 full suite 本地半边并誊输出摘要进 phase gate 段：quick run（analyze + test + verbatim_diff 全量）→ format 正名单 → `dart pub publish --dry-run` → example release 构建 + exe 存活断言
    2. 不变量 checklist 逐项落账：零 FRAME grep / channel 四锚 grep / linux+macos native 相对 Phase 1 基线零 diff（git diff <phase1-tag或commit> -- linux macos 为空）/ CMake 承重墙 =1 / 双 lockfile 平台锁定依赖零出现（XPLAT-02 术语，计数门见 automated）/ pubspec 白名单两项 / DEVIATIONS 行数对账（账面行数 = 5 + 修复行，每行与脚本豁免/断言一一对应）
    3. CI 最新 run 绿确认（gh -R 显式）
    4. WINDOWS.md 收口行：#4（resize 基线观察）标注"Phase 2 期间未复测，留 Phase 3 按账执行"；known-stub（example widget_test）状态确认；UAT 触发的新开放项（若有）追加
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2" && D:/flutter/bin/flutter analyze && D:/flutter/bin/flutter test && D:/flutter/bin/dart format --set-exit-if-changed --output=none test/channel_contract_test.dart example/lib/utils/window_event_logger.dart example/test/widget_test.dart && D:/flutter/bin/dart pub publish --dry-run && ! grep -rn "FRAME:" lib windows example test tool && [ "$(grep -c 'CMAKE_POLICY_VERSION_MINIMUM 3.5' windows/CMakeLists.txt)" -eq 1 ] && [ "$(grep -c win32 pubspec.lock)" -eq 0 ] && [ "$(grep -c win32 example/pubspec.lock)" -eq 0 ] && grep -c "window_frame_kit" lib/src/window_manager.dart windows/window_frame_kit_plugin.cpp linux/window_frame_kit_plugin.cc macos/window_frame_kit/Sources/window_frame_kit/WindowFrameKitPlugin.swift && gh run list -R TATboy210/window_frame_kit --workflow ci --limit 1 --json conclusion --jq '.[0].conclusion' | grep -qx success</automated>
    <fails_when>verbatim_diff 输出任何 "VERBATIM FAIL"/"residue N != 22"/"!= 2"（未记账偏离——先对 DEVIATIONS 账再判）；"No tests were found." 或通过数 <12（契约面缩水）；dry-run 非 "0 warnings"；`FRAME:` grep 有任何命中（phase 违规，立即停手上报——退出门条款）；承重墙/win32/白名单计数漂移；四锚 grep 任何文件计数 0（channel 名红线破损）；gh conclusion 非 success</fails_when>
  </verify>
  <done>SC5 终跑全绿 + 不变量 checklist 全项落账 + CI 绿 + WINDOWS.md 收口行——人工批③ 的机器前提全部成立</done>
  <acceptance_criteria>
    - phase gate 段含：verbatim_diff 输出行 "VERBATIM PROOF OK (17 dart files zero-diff, cpp residue 22+2)"、test 通过计数、dry-run 0 warnings、不变量 checklist 逐项 ✓、CI run id + conclusion
    - DEVIATIONS 对账行：账面每一行 ↔ 脚本断言/豁免的映射说明
    - git status --porcelain 仅 .planning/**
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 2: 批③ 实机 UAT checkpoint——SC4 全项 + 标题栏/拖拽/外观/穿透/菜单（CAPB-07~12）</name>
  <files>.planning/phases/02-windows/02-UAT-EVIDENCE.md（批③ 判定表 + sign-off）, .planning/WINDOWS.md（仅新开放项时）</files>
  <read_first>
    - 02-VALIDATION.md §Manual-Only Verifications 行 4/6/7（titleBarStyle / preventClose+destroy+popUpWindowMenu+拖拽 / 外观+穿透+tray）
    - 02-RESEARCH.md CAPB-07~12 行（每项 file:line 语义基准）+ Pitfall 7（setAsFrameless 顺序）+ Pitfall 9（物理 px / forward 忽略预期值）+ Pitfall 10（destroy 绕过 WM_CLOSE 语义）
    - example/lib/pages/home.dart（:1043-1047 tray 恢复菜单、:1056-1083 onWindowClose 对话框流——UAT 引导的操作位置）
  </read_first>
  <action>
    executor 拉起 `cd /d/window_frame_kit/example && D:/flutter/bin/flutter run -d windows`（控制台可见），按下方判定清单逐项引导；预期值全部按上游语义（Pitfall 9/10 纪律）；逐项记账进批③ 判定表。
  </action>
  <human_check>
    **判定清单（六组，逐项操作 → 用户目视/读控制台回答）：**
    1. **关闭拦截（CAPB-07/SC4）：** setPreventClose(true) → 点关闭按钮：窗口不消失 + 控制台 close 行 + example 确认对话框弹出 → 选 No 窗口留存 → 再点关闭选 Yes：进程退出（destroy 无条件终结、不触发 close 行 = Pitfall 10 上游语义，判"符合预期"）
    2. **标题栏样式（CAPB-08）：** setTitleBarStyle(hidden) → 目视无系统标题栏；setTitleBarStyle(normal) → 标题栏回来；getTitleBarHeight 控制台值 >0（150% DPI 下 ≈47 物理 px = 预期，不判缺陷）；windowButtonVisibility 参数 Windows 忽略 = 预期
    3. **拖拽发起（CAPB-09）：** DragToMoveArea 实拖窗口移动；DragToResizeArea 8 向逐一实拖；startDragging 等效；setFullScreen(true) 后再拖 = Dart 守卫拒发（无移动/无报错崩溃 = 预期）
    4. **外观 9 项（CAPB-10）：** setTitle/getTitle 回读、setIcon（tray/窗口图标换图）、setHasShadow、setOpacity（滑杆目视半透明）、setBackgroundColor（目视变色）、setBrightness（暗色标题栏切换）、setAlwaysOnTop（置其他窗口上）、setAlwaysOnBottom、getTitle 回读一致——逐项目视打勾
    5. **鼠标穿透（CAPB-11）：** setIgnoreMouseEvents(true) → 点击落到窗口下层（目视确认穿透）→ **tray 菜单恢复路径**（home.dart:1043-1047）把穿透关掉，窗口恢复可点击；forward:true 无任何效果 = 预期（Pitfall 9，Windows native 只读 ignore 键）
    6. **系统菜单（CAPB-12）：** setAsFrameless（在 waitUntilReadyToShow 完成后的规范顺序，Pitfall 7）→ popUpWindowMenu → 系统菜单在光标位弹出，移动/大小/最小化项可点且生效
    **通过标准：** 六组全"符合预期"（含三处上游语义预期项：destroy 不发 close / 物理 px / forward 无效）→ 批③ dated sign-off。
    **响应协议：** 用户回复 "approved"/逐项确认 → 记账；任何异常 → 记录症状原文 + 操作序列 + DPI 环境，对照 RESEARCH 对应 CAPB 行 file:line 定位（先分上游语义/环境/移植回归三类），移植回归修复过 Task 1 全套机器门 + DEVIATIONS 记账后**本批复验**；把预期项报成缺陷时引用 Pitfall 9/10 原文对照，必要时对照宿主 window_manager 现行为再判。
  </human_check>
  <verify>
    <automated>cd /d/window_frame_kit && grep -A120 "批③" .planning/phases/02-windows/02-UAT-EVIDENCE.md | grep -q "approved" && D:/flutter/bin/flutter analyze && D:/flutter/bin/flutter test && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2"</automated>
    <fails_when>账本批③ 段无 "approved"（未签核即收口）；quick run 红（批③ 若产生修复，17 文件零差 + 22/2 + 契约 ≥12 必须仍成立）；判定表有空行（六组漏项）</fails_when>
  </verify>
  <done>批③ 六组判定全落 + dated sign-off；SC4 全项签核成立</done>
  <acceptance_criteria>
    - 批③ 判定表：六组逐行含用户判定 + 日期；三处上游语义预期项显式标注"符合预期（Pitfall 9/10）"
    - CAPB-07~12 在批③ 段标记实机验证通过
    - 无回归情形 commit 仅含 .planning/**
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 3: Phase 退出门 checkpoint——SC1-4 全量复验矩阵 + 最终 sign-off（Phase 3 解锁裁决）</name>
  <files>.planning/phases/02-windows/02-UAT-EVIDENCE.md（phase gate 段：SC1-4 复验矩阵 + 最终 sign-off）</files>
  <read_first>
    - ROADMAP §Phase 2 Success Criteria 1-5（复验矩阵的行定义）+ 退出门条款原文
    - .planning/phases/02-windows/02-UAT-EVIDENCE.md（批①②③ + Task 1 机器门全部既有记录——复验矩阵是汇总核账不是重做）
    - 02-CONTEXT.md D-05（退出门 = 末批全量 UAT 的定案原文）
  </read_first>
  <action>
    1. 把 SC1-4 汇成复验矩阵（每 SC 一行：批①/②/③ 对应判定引用 + Task 1 机器证据引用 + 缺口标注），誊入 phase gate 段；SC5 行 = Task 1 终跑输出直接引用
    2. 请用户做**收口抽查**（非全量重做）：从 SC1-4 各挑 1 项现场复演（executor 拉起 example，用户目视）——验证账本记录与当下行为一致
    3. 呈交退出门裁决问题（见 human_check），获用户最终 sign-off
  </action>
  <human_check>
    **判定标准：**
    1. 复验矩阵无缺口行（每 SC 均有批判定引用 + 机器证据引用；任何"待补"行 = 门不成立）
    2. 收口抽查 4 项（SC1-4 各 1）现场复演与账本一致
    3. 零 `// FRAME:` 终态确认（Task 1 已机器证，向用户复述结论）
    4. **裁决问题（原文呈交）：** "Phase 2 退出门：window_manager 0.5.2 对等验证的 SC1-5 证据包已齐（矩阵 + 账本 + 机器门输出）。是否批准 Phase 2 结案、解除'对等验证通过之前不得开始任何嫁接'条款、解锁 Phase 3 frame 嫁接？"
    **响应协议：** 用户回复 "approved" → phase gate 段写入最终 dated sign-off + 裁决原文与答复 → commit 收口本计划；用户提出缺口/异常 → 矩阵标注缺口行，路由回对应批计划复验（02-03/04/05 的 checkpoint 重开），不得带缺口签核；抽查不一致 → 视为回归，走修复 + DEVIATIONS + 对应批复验后重开本 checkpoint。
  </human_check>
  <verify>
    <automated>cd /d/window_frame_kit && grep -q "phase gate" .planning/phases/02-windows/02-UAT-EVIDENCE.md && tail -40 .planning/phases/02-windows/02-UAT-EVIDENCE.md | grep -q "approved" && git log --oneline -3 && git status --porcelain | grep -v '.planning/' ; test $? -eq 1</automated>
    <fails_when>账本 phase gate 段缺最终 "approved"（退出门未获用户裁决即宣布结案 = 违反宿主 UAT 证据标准与 ROADMAP 退出门条款）；git status 出现 .planning/ 之外未提交条目（收口不干净）；复验矩阵存在缺口行</fails_when>
  </verify>
  <done>SC1-4 复验矩阵无缺口 + 收口抽查一致 + 用户最终裁决 approved 落账 + 账本 commit——Phase 2 退出门成立，Phase 3 解锁</done>
  <acceptance_criteria>
    - phase gate 段含：SC1-5 复验矩阵（5 行全 ✓）、抽查 4 项记录、裁决问答原文、最终 dated sign-off
    - CAPB-01~12 全部在账本中可追溯到实机判定行
    - 本 commit 后仓库工作区干净；CI 保持绿（无代码改动则沿用 Task 1 确认的 run）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 批①②③ 分散证据 → phase 退出门汇总 | 汇总核账可能掩盖单批缺口（矩阵缺口行 = 防线） |
| UAT 现场行为 ↔ 账本历史记录 | 收口抽查防"账实不符" |
| Phase 2 → Phase 3 交接 | 退出门 sign-off 是嫁接解锁的唯一授权点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-22 | Repudiation | 退出门无最终裁决记录 / 带缺口签核 | critical | mitigate | Task 3 裁决问答原文落账 + fails_when 盯 approved 与矩阵缺口行；ROADMAP 退出门条款引用入 human_check |
| T-02-23 | Tampering | 汇总期"顺手"修 quirk / 补 docked 发射 / 调格式破坏逐字终态 | high | mitigate | Task 1 机器门先于人工批（全量 verbatim + 零 FRAME + 四锚 + win32 + 承重墙一次锁死）；批③ 后 quick run 复跑 |
| T-02-24 | Spoofing | 账本记录与当下行为不符（陈旧 sign-off 冒充复验） | medium | mitigate | Task 3 收口抽查 SC1-4 各 1 项现场复演 |
| T-02-25 | DoS | 穿透 UAT 后窗口不可点击无法恢复（tray 路径失效） | medium | mitigate | tray_manager 依赖保留（02-01 落定）+ 批③ 判定 5 显式验证恢复路径；兜底 = 任务管理器结束进程（记 UAT 引导词） |
| T-02-26 | Elevation of Privilege | preventClose 无逃生门（用户被自己拦截锁死） | low | mitigate | SC4 判定 1 强制验证 destroy 确认路径（Pitfall 10 语义 + PITFALLS Security 表同款要求） |
</threat_model>

<verification>
Quick run（每 commit）：`cd /d/window_frame_kit && D:/flutter/bin/flutter analyze && D:/flutter/bin/flutter test && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2"`
Phase gate（本计划 Task 1+3）：full suite 本地半边 + 不变量 checklist + CI 绿 + SC1-4 复验矩阵 + 最终裁决 sign-off + 零 FRAME grep
</verification>

<success_criteria>
- SC4 全项实机签核（preventClose 拦截→destroy 逃生 / popUpWindowMenu frameless / 拖拽 / 外观 / 穿透+tray 恢复）
- SC5 终跑：17 Dart 文件零差 + cpp residue 22+2 + 契约全绿 + DEVIATIONS 对账 = 全自动证明
- SC1-4 复验矩阵无缺口 + 用户最终裁决 approved → **ROADMAP Phase 2 退出门成立，Phase 3 嫁接解锁**
- CAPB-01~12 全部实机验证闭环（批①01-03 / 批②04-06 / 批③07-12，tracer 02-02 打底）
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (本计划)
- UAT-EVIDENCE phase gate 段（SC1-5 复验矩阵 + 抽查记录 + 裁决问答 + 最终 sign-off）+ 批③ 段
- WINDOWS.md 收口行（#4 留 Phase 3 / known-stub 确认 / 新开放项若有）
- Phase 2 证据包全卷（02-UAT-EVIDENCE.md 三批 + gate 四段合一，供 Phase 3 planner 与 /gsd-verify-work 引用）
</artifacts_produced>

<output>
完成后创建 `.planning/phases/02-windows/02-05-SUMMARY.md`（含退出门裁决原文与 sign-off 引用）
</output>
