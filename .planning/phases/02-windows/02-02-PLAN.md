---
phase: 02-windows
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - windows/window_frame_kit_plugin.cpp   # 覆盖：WM 597 行 verbatim + 3 处适配
  - windows/window_manager.cpp            # 新增：WM 1129 行 verbatim + L1 include（保留上游文件名，Open Q1 定案）
  - windows/window_frame_kit_plugin.h     # 覆盖：骨架头 → 最小桥接头（21 行，新码区）
  - windows/CMakeLists.txt                # 合并：PLUGIN_SOURCES + 双 define；承重墙行保留
  - windows/test/window_frame_kit_plugin_test.cpp  # 覆盖：模板测试 → gtest 占位（新码区）
  - tool/verbatim_diff.sh                 # 扩展 B 段（C++ 精确行数断言 22/2）
autonomous: false
requirements: [CAPB-01, CAPB-04, CAPB-05, CAPB-06]
estimate:
  tokens: 55000
  raw_tokens: 38000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "本机 `flutter build windows --release`（example 宿主）通过，产出 window_frame_kit_example.exe；gtest 目标（window_frame_kit_test.vcxproj）同构建绿——A4 在 D:/ 真实路径复现 probe 结论"
    - "C++ 残留 diff 精确封顶：window_frame_kit_plugin.cpp = 恰 22 行（3 hunk：L1 include / L112 channel 串 / 尾块 extern-C→桥接）、window_manager.cpp = 恰 2 行（1 hunk：L1 include）——两数字即 SC5 C++ 半边的机器门"
    - "匿名命名空间内部标识符零改名：WindowManagerPlugin / WindowManager / window_manager 成员等 113 处含 window_manager 的行全部原样（D-01 C++ 侧执行形态）"
    - "tracer 实机成立：example 启动 → 窗口按 WindowOptions 出现 + 控制台出现 WindowEventLogger 事件行 = ensureInitialized → waitUntilReadyToShow → native 事件回路全通（CAPB-01 tracer 证据，probe 已预演 6s Responding）"
    - "注册链四锚不动：registrant → plugin_c_api.cpp → window_frame_kit::WindowFrameKitPlugin::RegisterWithRegistrar → WindowManagerPlugin::RegisterWithRegistrar（桥接 8 行，Pitfall 4 定案）"
  artifacts:
    - windows/window_frame_kit_plugin.cpp（WM 597 行 verbatim + 桥接尾块，`// DEV:` 标记）
    - windows/window_manager.cpp（WM 1129 行 verbatim，保留上游文件名）
    - windows/window_frame_kit_plugin.h（最小桥接头，新码区）+ windows/test/window_frame_kit_plugin_test.cpp（gtest 占位，`// DEV:` 注释）
    - windows/CMakeLists.txt 合并版（PLUGIN_SOURCES 3 源 + PLUGIN_NAME/TEST_RUNNER 双 _SILENCE_CXX17_CODECVT_HEADER_DEPRECATION_WARNING + 承重墙行原样）
    - tool/verbatim_diff.sh B 段（22/2 精确断言）
  key_links:
    - "generated_plugin_registrant.cc → WindowFrameKitPluginCApiRegisterWithRegistrar（include/window_frame_kit/window_frame_kit_plugin_c_api.h）→ plugin_c_api.cpp 薄壳 → 桥接头声明 → plugin.cpp 匿名命名空间实现"
    - "plugin.cpp:15 `#include \"window_manager.cpp\"`（上游原样，保留文件名的理由）→ CMake PLUGIN_SOURCES 含 window_manager.cpp"
    - "example main.dart → channel 'window_frame_kit'（plugin.cpp L112 适配点）→ native 61 方法分支 + _EmitEvent → logger 控制台行"
  prohibitions:
    - statement: "零嫁接码：WM_GETMINMAXINFO 协作式覆盖（plugin.cpp:190-205）、NCCALCSIZE hidden 分支（plugin.cpp:165-185）、WM_NCHITTEST 均为 Phase 3 禁改区——verbatim 进驻不得触碰；不得出现 `// FRAME:` 标记"
      status: resolved
      verification: "! grep -rn 'FRAME:' lib windows example test tool 且 verbatim_diff.sh B 段 22/2 通过（行数漂移=禁改区被触碰）"
    - statement: "壳三件不动：window_frame_kit_plugin_c_api.cpp、include/window_frame_kit/window_frame_kit_plugin_c_api.h、pubspec pluginClass=WindowFrameKitPluginCApi（registrant 四锚，Pitfall 4）"
      status: resolved
      verification: "git diff --stat 本 commit 不含 plugin_c_api.cpp / include/**/c_api.h / pubspec.yaml"
    - statement: "CMake 承重墙行不得删除：`set(CMAKE_POLICY_VERSION_MINIMUM 3.5)` 保持在 windows/CMakeLists.txt（gtest FetchContent 前）；gtest 块不删（policy 行的存在前提）"
      status: resolved
      verification: "grep -c 'CMAKE_POLICY_VERSION_MINIMUM 3.5' windows/CMakeLists.txt = 1"
    - statement: "不修上游 quirk：IsWindows11OrGreater 恒真（plugin.cpp:31）、getTitleBarHeight 物理 px、codecvt 弃用用法等全部 verbatim；taskbar_ 空指针时序不加空判（Pitfall 6/8/9——加了=偏离）"
      status: resolved
      verification: "verbatim_diff.sh B 段残留行数恰 22/2（任何 quirk 修改都会推高行数）"
    - statement: "channel 名红线：plugin.cpp 内 channel 串只允许 'window_frame_kit'（L112 适配点）；不得引入 'window_manager' channel 串"
      status: resolved
      verification: "grep -c \"window_frame_kit\" windows/window_frame_kit_plugin.cpp ≥1 且 grep -n \"L\\\"window_manager\\\"\\|'window_manager'\" windows/window_frame_kit_plugin.cpp = 空"
    - statement: "不动区：linux/ macos/ 模板壳（F-4）、Dart lib/（02-01 已 verbatim 锁定）、example runner/CMake（裸模板 = HandleTopLevelWindowProc 转发链前提，Pattern 1）"
      status: resolved
      verification: "git diff --stat 本 commit 仅含 windows/** + tool/verbatim_diff.sh"
---

<objective>
把 window_manager 0.5.2 的 Windows C++ 面（plugin.cpp 597 行 + window_manager.cpp 1129 行）以"骨架 C-API 壳 + 匿名命名空间 verbatim 进驻 + 8 行桥接"定案（RESEARCH Pattern 1，probe 构建+启动双证）移植进本包，随后用实机 tracer checkpoint 证明初始化协议 + 事件回路端到端走通——本 phase 的 tracer 纵切在此收口。

**用户故事（planner 按 ROADMAP Goal 框定）：** 作为 Flutter 桌面开发者，我要 example 窗口在本包 native 实现下按 WindowOptions 真实出现并打出事件日志，以便确信 Dart facade ↔ native 的对等基座已经通电，Phase 3 嫁接有可分辨的回归参照。

**Purpose:** SC1 的 tracer 半边（窗口按参数出现）+ CAPB-01/06 的 native 接通；SC5 C++ 半边（22/2 精确断言）。
**Output:** commit「feat(02-02): port window_manager 0.5.2 Windows C++ surface verbatim (C-API bridge)」+ 实机 tracer checkpoint dated sign-off。
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-windows/02-CONTEXT.md
@.planning/phases/02-windows/02-RESEARCH.md
@.planning/phases/02-windows/02-01-SUMMARY.md
移植源（只读）：`C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2/windows/`
</context>

<tasks>

<task type="tracer">
  <name>Task 1: C++ 面移植（verbatim 进驻 + 3 处适配 + 桥接头 + CMake 合并 + gtest 占位 + diff 脚本 B 段）→ 本机构建绿</name>
  <files>windows/window_frame_kit_plugin.cpp, windows/window_manager.cpp, windows/window_frame_kit_plugin.h, windows/CMakeLists.txt, windows/test/window_frame_kit_plugin_test.cpp, tool/verbatim_diff.sh</files>
  <read_first>
    - 02-RESEARCH.md §Code Examples Example 4（桥接头全文 + 尾块桥接全文 + CMake 合并增量，probe 构建+启动实证的精确形态）+ Pattern 1（注册链图）+ Pitfall 4（壳结构错配）+ Pitfall 5（codecvt /W4 /WX：PLUGIN_NAME 与 TEST_RUNNER 双 define）+ §File-by-File Port Map B（残留 diff 行数值门）+ Open Question 1（window_manager.cpp 保留上游名定案）
    - 移植源：window_manager-0.5.2/windows/window_manager_plugin.cpp（597 行，L1/L112/L592-597 三个适配点）、windows/window_manager.cpp（1129 行，仅 L1）、windows/CMakeLists.txt（:10-17 合并参照）
    - 现骨架：windows/window_frame_kit_plugin.cpp、windows/window_frame_kit_plugin.h、windows/CMakeLists.txt（L17 PLUGIN_NAME / L20-23 PLUGIN_SOURCES / L81 承重墙行 / L86-89 TEST_RUNNER 直编 PLUGIN_SOURCES）、windows/test/window_frame_kit_plugin_test.cpp（模板 GetPlatformVersion 测试——桥接方案下不可编译，必须替换）、windows/window_frame_kit_plugin_c_api.cpp（12 行薄壳，不动，读一遍确认转发目标名）
  </read_first>
  <action>
    1. cp 上游 windows/window_manager_plugin.cpp → windows/window_frame_kit_plugin.cpp（覆盖骨架版），随后恰 3 处适配：L1 include 改 `"window_frame_kit_plugin.h"`；L112 channel 串 `'window_manager'`→`'window_frame_kit'`（宽/窄串按上游原形态只换名）；L592-597 extern "C" 导出块整体替换为 Example 4 的 14 行桥接块（namespace window_frame_kit + `// DEV:` 注释 + `WindowFrameKitPlugin::RegisterWithRegistrar` 转发 `WindowManagerPlugin::RegisterWithRegistrar`）。多行块替换用 python assert-in + replace（sed 跨行易碎，probe 实证）。匿名命名空间内 113 处 window_manager 标识符行**零改名**
    2. cp 上游 windows/window_manager.cpp → windows/window_manager.cpp（保留上游文件名，Open Q1 定案），仅 L1 include 改 `"window_frame_kit_plugin.h"`
    3. windows/window_frame_kit_plugin.h 覆盖为 Example 4 最小桥接头（21 行新码区：include guard + plugin_registrar_windows.h + namespace window_frame_kit + 静态 RegisterWithRegistrar 声明 + `// DEV:` 注释指向 DEVIATIONS.md）
    4. windows/CMakeLists.txt 骨架基底合并（Example 4 CMake 段）：PLUGIN_SOURCES 增 `"window_manager.cpp"`；`target_compile_definitions(${PLUGIN_NAME} PRIVATE _SILENCE_CXX17_CODECVT_HEADER_DEPRECATION_WARNING)` 与 `target_compile_definitions(${TEST_RUNNER} PRIVATE ...)` 两目标都加（Pitfall 5）；L81 承重墙行与 gtest 块、PLUGIN_NAME、include dirs、bundled_libraries、c_api 源全保留骨架形态
    5. windows/test/window_frame_kit_plugin_test.cpp 覆盖为 gtest 占位测试（新码区：一个 EXPECT_TRUE(true) 级占位 + `// DEV:` 注释说明"WM 类构造需活 registrar（上游因此无 native 测试）；占位保活 gtest 机制与 policy 补丁给 Phase 3 纯函数测试用"）
    6. tool/verbatim_diff.sh 追加 B 段（Example 2 的 n1/n2 断言）：`n1=22`（plugin.cpp）/`n2=2`（window_manager.cpp）精确行数，末尾输出改 `VERBATIM PROOF OK (17 dart files zero-diff, cpp residue 22+2)`；DEVIATIONS.md 02-01 已记账的 ②③④ 行在本任务落码（账实对齐，无需新增行；若实测行数≠22/2，先停手对照 RESEARCH 再记账）
    7. 构建：`cd /d/window_frame_kit/example && D:/flutter/bin/flutter build windows --release`（A4 验证点：probe 在 C:\...\Temp 通过，本步在 D:/ 真实路径复现；MSB8029 临时目录警告为 probe-only 不应出现）
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2" && (cd example && D:/flutter/bin/flutter build windows --release) && test -f example/build/windows/x64/runner/Release/window_frame_kit_example.exe && echo BUILD_OK && [ "$(grep -c 'CMAKE_POLICY_VERSION_MINIMUM 3.5' windows/CMakeLists.txt)" -eq 1 ] && [ "$(grep -c '_SILENCE_CXX17_CODECVT_HEADER_DEPRECATION_WARNING' windows/CMakeLists.txt)" -eq 2 ] && [ -z "$(git status --porcelain | grep -v -E 'windows/|tool/verbatim_diff.sh')" ]</automated>
    <fails_when>脚本输出 "plugin.cpp residue N != 22" 或 "window_manager.cpp residue N != 2"（适配点做多/做少，或禁改区被触碰）或任何 "VERBATIM FAIL"；build 输出 "warning C4996 ... STL4017" + MSB 失败指向 window_frame_kit_test.vcxproj（Pitfall 5：TEST_RUNNER define 漏加）；链接错误 "未解析的 WindowFrameKitPluginCApiRegisterWithRegistrar"（Pitfall 4：桥接目标名与 plugin_c_api.cpp 转发名不一致）；exe 不存在；policy 行计数 ≠1（承重墙被删）；define 计数 ≠2；git status --porcelain 出现 windows//tool 之外的条目（越界改动）</fails_when>
  </verify>
  <done>WM C++ 1726 行 verbatim 进驻 + 3 适配点 + 桥接三件 + CMake 合并落定；diff 脚本全量（17 Dart 零差 + 22/2）通过；example release 构建绿含 gtest 目标</done>
  <acceptance_criteria>
    - verbatim_diff.sh 输出 "VERBATIM PROOF OK (17 dart files zero-diff, cpp residue 22+2)"
    - grep -c "window_manager" windows/window_frame_kit_plugin.cpp ≥ 100（内部标识符零改名的粗证；精确面由 22 行 diff 封顶）
    - windows/window_frame_kit_plugin.h ≤ 25 行且含 "// DEV:" 与 "static void RegisterWithRegistrar(flutter::PluginRegistrarWindows"
    - windows/CMakeLists.txt 的 PLUGIN_SOURCES 含 window_manager.cpp / window_frame_kit_plugin.cpp / window_frame_kit_plugin.h 三源
    - plugin_c_api.cpp、include/**/c_api.h、pubspec.yaml、linux/**、macos/** 零改动
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: exe 启动存活断言 + commit + push + CI（windows leg 重点）绿确认</name>
  <files>（无新文件改动；git 操作 + 门复跑）</files>
  <read_first>
    - Phase 1 proven commands（exe 存活 powershell 断言、CI gh -R 显式仓库）
    - 02-VALIDATION.md §Sampling Rate（P02 起 plan 收口加 exe 启动存活断言）
  </read_first>
  <action>
    1. 启动 exe（`example/build/windows/x64/runner/Release/window_frame_kit_example.exe` 后台拉起），数秒后 powershell Get-Process 断言存活，然后关闭清理（探针式存活验证；完整实机目视留给 Task 3 checkpoint，此处只看"进程不早退不崩溃"——注册链断链会表现为启动即 MissingPluginException 或早退）
    2. 单 commit：`feat(02-02): port window_manager 0.5.2 Windows C++ surface verbatim (C-API bridge)`（含 windows/ 五文件 + verbatim_diff.sh B 段）
    3. push；CI 确认（windows leg 为本 commit 重点：native build 含 gtest 门开构建；ubuntu leg verbatim-diff 步此时升级为 Dart+C++ 全量断言——脚本 B 段对 CI 解包目录同样生效）
  </action>
  <verify>
    <automated>cd /d/window_frame_kit && (example/build/windows/x64/runner/Release/window_frame_kit_example.exe &) && sleep 6 && powershell -NoProfile -Command "if (Get-Process window_frame_kit_example -ErrorAction SilentlyContinue) { exit 0 } else { exit 1 }" && powershell -NoProfile -Command "Stop-Process -Name window_frame_kit_example -ErrorAction SilentlyContinue" && git remote get-url origin | grep -q "github.com[:/]TATboy210/window_frame_kit" && gh run list -R TATboy210/window_frame_kit --workflow ci --limit 1 --json conclusion --jq '.[0].conclusion' | grep -qx success</automated>
    <fails_when>Get-Process 断言 exit 1（进程 6 秒内消失=注册链断/native crash——先查桥接目标名一致性与 Pitfall 4 链接症状，再看 Windows 事件查看器）；gh conclusion 非 success 且 windows leg 红（读本机与 CI 的 VS/CMake 版本差——Phase 1 A2 结论：windows-latest=VS2026+CMake4，policy 补丁必需）；ubuntu leg verbatim-diff 步红（B 段行数断言在 archive 对照下不成立=本地 pub cache 与 archive 字节差异，停手上报）</fails_when>
  </verify>
  <done>exe 6 秒存活断言通过并清理、单 commit push、CI run conclusion=success（四 job 全绿）</done>
  <acceptance_criteria>
    - 存活断言 exit 0；进程已清理（无残留 window_frame_kit_example 进程）
    - git log -1 --stat 仅含 windows/ 五文件 + tool/verbatim_diff.sh
    - CI windows leg native build 日志无 C4996；ubuntu leg verbatim-diff 输出含 "cpp residue 22+2"
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 3: 实机 tracer checkpoint——窗口按 WindowOptions 出现 + 控制台事件回路首证（CAPB-01 tracer）</name>
  <files>.planning/phases/02-windows/02-02-SUMMARY.md（checkpoint 结果记录处）</files>
  <read_first>
    - 02-RESEARCH.md Pattern 5 P02 行（tracer 定义）+ Example 5（触发-事件对照表）+ Pitfall 6（taskbar_ 时序：UAT 只按 example 规范序列操作，不裸调 setSkipTaskbar）
    - 02-VALIDATION.md §Manual-Only Verifications 行 1（窗口按 WindowOptions 出现）
    - example/lib/main.dart（WindowOptions 参数实值 = 目视判定基准）+ example/lib/utils/window_event_logger.dart（日志行格式）
  </read_first>
  <action>
    引导用户实机执行（executor 拉起 `cd example && D:/flutter/bin/flutter run -d windows`，控制台朝向用户可见）并逐项请用户确认。宿主 UAT 证据标准：控制台无崩溃 ≠ UI 正确呈现——窗口观感必须用户目视，事件行以控制台日志为充分证据。
  </action>
  <human_check>
    **判定清单（逐项请用户目视/读控制台后回答）：**
    1. 窗口出现且尺寸/位置符合 example main.dart 的 WindowOptions 实值（对照源码读数：预期 800×600、屏幕居中）——用户确认"是/否 + 实际观感"
    2. 标题栏观感符合 WindowOptions titleBarStyle 设定（hidden = 无系统标题栏）——用户目视确认
    3. 控制台出现 `[window_frame_kit] {时间戳} ...` 格式的事件行：启动/点击窗口至少见 focus（及点别处 blur）——用户读控制台确认事件名
    4. 点击关闭按钮：控制台出现 close 事件行且 example 弹出确认对话框（preventClose 上游默认路径），选 No 窗口留存——用户确认
    **通过标准：** 4 项全"是" → 记录 dated sign-off（tracer = 初始化协议 + 事件回路端到端成立）。
    **响应协议：** 用户回复 "approved" → 记入 SUMMARY 后收口本计划；用户描述任何异常（窗口不现/尺寸不对/标题栏不对/无事件行/关闭即退）→ 不猜测不静默：记录症状原文，对照 RESEARCH Pitfall 4（注册链）/6（时序）/7（is_frameless_ 顺序）定位，修复走 DEVIATIONS 记账 + 复验，重大偏离上报用户裁决。
  </human_check>
  <verify>
    <automated>cd /d/window_frame_kit && test -f example/build/windows/x64/runner/Release/window_frame_kit_example.exe && grep -q "approved" .planning/phases/02-windows/02-02-SUMMARY.md</automated>
    <fails_when>SUMMARY 无 "approved" 字样（checkpoint 未获用户签核即收口=违反宿主 UAT 证据标准）；exe 缺失（未构建就跑 checkpoint）</fails_when>
  </verify>
  <done>用户对 4 项判定清单逐项确认，dated sign-off 写入 02-02-SUMMARY.md；tracer 纵切（Dart facade ↔ native ↔ 窗口呈现 ↔ 事件日志）端到端成立</done>
  <acceptance_criteria>
    - SUMMARY 含：4 项判定逐项结果 + 用户原话/dated sign-off + 任何异常的处置记录
    - checkpoint 通过后方可进入 02-03（批① UAT 的窗口/事件基线在本 tracer 已立）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub cache C++ 源 → windows/ 逐字区 | 597+1129 行 native 代码进驻（供应链完整性 + 禁改区纪律） |
| Dart facade ↔ native channel | L112 channel 串是两侧唯一契约点 |
| 构建管线（MSVC /W4 /WX + gtest FetchContent） | 警告即错误 + 承重墙 policy 行是构建存活前提 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | Phase 3 禁改区（plugin.cpp:165-185 NCCALCSIZE / :190-205 GETMINMAXINFO / WM_NCHITTEST）在本移植中被"顺手"触碰 | critical | mitigate | 残留 diff 精确封顶 22/2（verbatim_diff.sh B 段，CI 持续门）——任何禁改区改动立即推高行数红灯 |
| T-02-09 | Tampering | 注册链锚（plugin_c_api.cpp / c_api.h / pubspec pluginClass / registrant）被上游单类注册方式覆盖（Pitfall 4） | high | mitigate | 壳三件列入 files_modified 禁区 + Task 1 git diff --stat 越界断言；链接错误症状写入 fails_when |
| T-02-10 | DoS | 构建管线（codecvt C4996 × /W4 /WX；gtest × CMake 4.x） | medium | mitigate | 双目标 silence define（Pitfall 5）+ 承重墙 policy 行 grep 门 =1 |
| T-02-11 | Spoofing | channel 串漂移/复用 'window_manager'（双包共存宿主内消息串台） | medium | mitigate | L112 唯一适配点 + Task 1 grep 断言 + 契约测试（02-01）Dart 侧锚定 |
| T-02-12 | Repudiation | 实机 tracer 结果无据可查（"跑过了"但无签核） | medium | mitigate | Task 3 human_check dated sign-off 强制入 SUMMARY；fails_when 盯 "approved" 字样 |
| T-02-13 | Elevation of Privilege | native 面新增能力（destroy/PostQuitMessage、注册表读取 setBrightness） | low | accept | 全部上游 verbatim（对等移植非新增面）；destroy 逃生门语义 02-05 SC4 UAT 验证 |
</threat_model>

<verification>
Quick run（每 task commit）：`cd /d/window_frame_kit && D:/flutter/bin/flutter analyze && D:/flutter/bin/flutter test && bash tool/verbatim_diff.sh "C:/Users/35490/AppData/Local/Pub/Cache/hosted/pub.dev/window_manager-0.5.2"`
Plan 收口：+ example release 构建 + exe 存活断言 + CI 三 leg 绿 + tracer checkpoint dated sign-off
</verification>

<success_criteria>
- SC5 C++ 半边成立：残留 diff 恰 22+2，DEVIATIONS ②③④ 行账实对齐
- SC1 tracer 半边成立：窗口按 WindowOptions 出现 + 事件日志首行（实机签核）
- CAPB-01 native 接通；CAPB-04/05/06 的 native 实现进驻（行为验证在 02-03/04）
- 注册链四锚不动、承重墙行保留、壳三件零改动
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (本计划)
- windows/window_frame_kit_plugin.cpp（597 行 verbatim + 桥接尾块）· windows/window_manager.cpp（1129 行 verbatim）
- windows/window_frame_kit_plugin.h（桥接头）· windows/test/window_frame_kit_plugin_test.cpp（gtest 占位）
- windows/CMakeLists.txt 合并版 · tool/verbatim_diff.sh B 段
</artifacts_produced>

<output>
完成后创建 `.planning/phases/02-windows/02-02-SUMMARY.md`（含 tracer checkpoint 的 dated sign-off）
</output>
