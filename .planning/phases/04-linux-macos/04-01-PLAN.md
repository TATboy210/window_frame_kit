---
phase: 04-linux-macos
plan: 01
status: planned
requirements: [XPLAT-01, XPLAT-04, XPLAT-05]
---

# Linux/macOS 最小移植

## 已授权范围

2026-09-05用户允许以Phase2机器基座f192dd5开始Phase3/4，人工集中最终验收。优先复制window_manager0.5.2成熟实现，不重写窗口状态机。

## 步骤

1. 保留现有三平台模板注册名，复制上游Linux插件实现并机械归一包名、GObject类名/宏；注册符号为snake_case。保留GTK CMake、模板native测试并适配测试，不误删已有文件。
2. 复制macOS WindowManager与插件分发实现，插件类/通道改名；保留现有FlutterFramework SwiftPM依赖布局与隐私文件。部署下限以当前Flutter模板macOS12为准，与podspec一致，不凭旧上游10.11/10.15虚报支持。
3. 与Phase3统一setCustomFrame通道，明确保存/恢复原装饰状态；不把无支持的方法假装成功。README平台矩阵列支持/不支持/待实测。
4. 对机械移植建立可复现diff检查，Dart契约验证全部已有平台API继续可调用；macOS/Linux实际构建仅由对应CI证明。

## 门与退出条件

Windows回归保持通过；Linux/macOS CI原生编译通过才声称能构建。macOS获焦/拖动、Linux X11及Wayland无边框/缩放保留人工债。未验证不得标Phase4完成；不发布pub.dev、不自动改宿主。
